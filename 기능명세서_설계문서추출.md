
# Vision-Language GUI QA Agent 기능 명세서
**담당 파트:** ① 설계 문서 추출 모듈 개발

## 1. 개요
본 모듈은 Vision-Language 웹 GUI QA Agent 프로젝트의 첫 번째 단계로, 기획자 및 디자이너가 작성한 Figma 설계 문서를 AI가 이해할 수 있는 구조화된 데이터로 변환하는 역할을 수행합니다.

## 2. 주요 기능
* **Figma API 연동:** 인증 토큰과 파일 키를 활용하여 외부 Figma 서버와 HTTP GET 통신을 수행합니다.
* **UI 요소 자동 필터링:** 수신된 대규모 원본 데이터에서 'button' 및 'input' 키워드가 포함된 핵심 UI 요소만 선별합니다.
* **좌표 및 속성 추출:** 탐지된 요소의 화면상 절대 좌표, ID, 타입, 이름을 추출합니다.
* **테스트 요구사항 매핑:** 추출된 UI 요소에 입력 조건, 에러 메시지, 성공 조건을 매핑하여 완전한 테스트 시나리오 기반 데이터를 생성합니다.

## 3. 입출력 데이터 규격 (I/O)
* **Input (입력):** Figma Document URL (File Key), Figma Personal Access Token
* **Output (출력):** final_extracted_data.json

