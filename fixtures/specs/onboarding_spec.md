# 화면기획서 — 가입부터 첫 상품 등록까지

문서번호: WITCHES-SCR-020
작성일: 2026-08-26
작성자: 기획팀

이 문서는 신규 사용자가 처음 들어와 상품을 하나 등록하기까지 지나는 화면을
한 문서에 담는다. 화면마다 페이지를 새로 시작한다.

상품 등록 화면은 로그인한 사용자만 들어갈 수 있다. 그래서 이 문서의 화면들은
따로 볼 때보다 **이어서 볼 때** 확인되는 것이 더 많다.

## 1. 화면 개요

| 항목 | 내용 |
|---|---|
| 화면 ID | signup |
| 화면명 | 회원가입 |
| 화면 경로 | /signup |
| 설명 | 신규 사용자가 계정 정보를 입력해 회원으로 등록하는 화면 |

## 2. UI 요소 정의

| 요소 ID | 유형 | 라벨 | 필수 | 입력 검증 규칙 | 안내 문구 | 에러 메시지 |
|---|---|---|---|---|---|---|
| email | 입력 | 이메일 | 필수 | 이메일 형식(@ 포함) | 이메일을 입력하세요 | 올바른 이메일 형식을 입력하세요. |
| password | 입력 | 비밀번호 | 필수 | 8자 이상, 대문자 1자 이상, 특수문자 1자 이상 | 비밀번호를 입력하세요 | 비밀번호는 8자 이상이며 대문자·특수문자를 각 1자 이상 포함해야 합니다. |
| password_confirm | 입력 | 비밀번호 확인 | 필수 | 비밀번호와 동일한 값 | 비밀번호를 다시 입력하세요 | 비밀번호가 일치하지 않습니다. |
| nickname | 입력 | 닉네임 | 필수 | 2자 이상 10자 이하 | 닉네임을 입력하세요 | 닉네임은 2자 이상 10자 이하로 입력하세요. |
| signup_path | 선택 | 가입 경로 | 필수 | 선택 목록: 검색, 지인 추천, 광고 | - | 가입 경로를 선택하세요. |
| agree_terms | 체크박스 | 약관 동의 | 필수 | - | - | 약관에 동의해야 합니다. |
| signup_btn | 버튼 | 가입하기 | - | - | - | - |
| to_login | 링크 | 로그인하러 가기 | - | - | - | - |

### 2-1. 입력 검증 규칙 상세

**이메일**
- 필수 입력이다. 비어 있으면 에러 메시지를 노출한다.
- `@` 를 포함한 이메일 형식이어야 한다. 형식이 맞지 않으면 "올바른 이메일 형식을 입력하세요." 를 노출한다.

**비밀번호**
- 필수 입력이다. 비어 있으면 에러 메시지를 노출한다.
- 최소 길이는 8자다.
- 영문 대문자를 1자 이상 포함해야 한다.
- 특수문자를 1자 이상 포함해야 한다.
- 위 규칙 중 하나라도 위반하면 "비밀번호는 8자 이상이며 대문자·특수문자를 각 1자 이상 포함해야 합니다." 를 노출한다.

**비밀번호 확인**
- 필수 입력이다. 비어 있으면 에러 메시지를 노출한다.
- 비밀번호 항목에 입력한 값과 완전히 같아야 한다. 다르면 "비밀번호가 일치하지 않습니다." 를 노출한다.

**닉네임**
- 필수 입력이다. 비어 있으면 에러 메시지를 노출한다.
- 최소 길이는 2자, 최대 길이는 10자다.
- 길이 규칙을 위반하면 "닉네임은 2자 이상 10자 이하로 입력하세요." 를 노출한다.

**가입 경로**
- 필수 선택이다. 선택 목록은 검색, 지인 추천, 광고 세 가지다.
- 선택하지 않으면 "가입 경로를 선택하세요." 를 노출한다.

**약관 동의**
- 필수 항목이다. 체크하지 않으면 "약관에 동의해야 합니다." 를 노출한다.

## 3. 성공 조건

모든 입력 검증 규칙을 만족하면 `/welcome` 으로 이동하고
"가입이 완료되었습니다" 문구를 노출한다.
그 화면에 '로그인하러 가기' 링크를 두어 로그인 화면으로 이동할 수 있게 한다.

## 4. 실패 조건

| 상황 | 처리 |
|---|---|
| 필수 입력값이 비어 있음 | "필수 입력 항목입니다." 노출, 이동하지 않음 |
| 이메일 형식이 올바르지 않음 | "올바른 이메일 형식을 입력하세요." 노출, 이동하지 않음 |
| 비밀번호 검증 규칙 위반 | "비밀번호는 8자 이상이며 대문자·특수문자를 각 1자 이상 포함해야 합니다." 노출, 이동하지 않음 |
| 비밀번호 확인이 비밀번호와 다름 | "비밀번호가 일치하지 않습니다." 노출, 이동하지 않음 |
| 닉네임 길이 규칙 위반 | "닉네임은 2자 이상 10자 이하로 입력하세요." 노출, 이동하지 않음 |
| 가입 경로를 선택하지 않음 | "가입 경로를 선택하세요." 노출, 이동하지 않음 |
| 약관에 동의하지 않음 | "약관에 동의해야 합니다." 노출, 이동하지 않음 |

## 5. 입력 예시 데이터

| 이메일 | 비밀번호 | 닉네임 |
|---|---|---|
| newuser@test.com | Signup1! | 테스터 |

<!-- page -->

## 1. 화면 개요

| 항목 | 내용 |
|---|---|
| 화면 ID | login |
| 화면명 | 로그인 |
| 화면 경로 | /login |
| 설명 | 기존 회원이 이메일과 비밀번호를 입력해 서비스에 로그인하는 화면 |

## 2. UI 요소 정의

| 요소 ID | 유형 | 라벨 | 필수 | 입력 검증 규칙 | 안내 문구 | 에러 메시지 |
|---|---|---|---|---|---|---|
| email | 입력 | 이메일 | 필수 | 이메일 형식(@ 포함) | 이메일을 입력하세요 | 올바른 이메일 형식을 입력하세요. |
| password | 입력 | 비밀번호 | 필수 | 8자 이상, 대문자 1자 이상, 특수문자 1자 이상 | 비밀번호를 입력하세요 | 비밀번호는 8자 이상이며 대문자·특수문자를 각 1자 이상 포함해야 합니다. |
| login_btn | 버튼 | 로그인 | - | - | - | - |

### 2-1. 입력 검증 규칙 상세

**이메일**
- 필수 입력이다. 비어 있으면 에러 메시지를 노출한다.
- `@` 를 포함한 이메일 형식이어야 한다. 형식이 맞지 않으면 "올바른 이메일 형식을 입력하세요." 를 노출한다.

**비밀번호**
- 필수 입력이다. 비어 있으면 에러 메시지를 노출한다.
- 최소 길이는 8자다.
- 영문 대문자를 1자 이상 포함해야 한다.
- 특수문자를 1자 이상 포함해야 한다.
- 위 규칙 중 하나라도 위반하면 "비밀번호는 8자 이상이며 대문자·특수문자를 각 1자 이상 포함해야 합니다." 를 노출한다.

## 3. 성공 조건

이메일과 비밀번호가 모든 입력 검증 규칙을 만족하고 등록된 계정 정보와 일치하면,
`/dashboard` 로 이동하고 "환영합니다" 문구를 노출한다.

## 4. 실패 조건

| 상황 | 처리 |
|---|---|
| 필수 입력값이 비어 있음 | "필수 입력 항목입니다." 노출, 이동하지 않음 |
| 이메일 형식이 올바르지 않음 | "올바른 이메일 형식을 입력하세요." 노출, 이동하지 않음 |
| 비밀번호 검증 규칙 위반 | "비밀번호는 8자 이상이며 대문자·특수문자를 각 1자 이상 포함해야 합니다." 노출, 이동하지 않음 |
| 등록되지 않은 계정 | "이메일 또는 비밀번호가 올바르지 않습니다." 노출, 이동하지 않음 |

## 5. 테스트 계정

| 이메일 | 비밀번호 |
|---|---|
| user@test.com | Abcd123! |

## 6. 예시 동작

입력 검증은 통과하지만 등록되지 않은 계정인 경우를 예시로 제시한다.
이 상황은 값의 규칙만으로는 만들 수 없다 — 규칙을 다 지킨 값이기 때문이다.

| 이메일 | 비밀번호 | 노출돼야 하는 문구 |
|---|---|---|
| nobody@test.com | Abcd123! | 이메일 또는 비밀번호가 올바르지 않습니다. |

<!-- page -->

## 1. 화면 개요

| 항목 | 값 |
|---|---|
| 화면 ID | product |
| 화면 이름 | 상품 등록 |
| 경로 | /product |

## 2. UI 요소 정의

| 요소 ID | 유형 | 라벨 | 필수 | 검증 규칙 | 에러 메시지 | 안내 문구 |
|---|---|---|---|---|---|---|
| name | input | 상품명 | Y | 최대 50자 | 상품명은 50자 이내로 입력해주세요. | 상품명을 입력하세요 |
| price | input | 가격 | Y | 숫자만 입력 가능 | 가격은 숫자만 입력할 수 있습니다. | 숫자만 입력 |
| stock | input | 재고수량 | Y | 숫자만 입력 가능 | 재고수량은 숫자만 입력할 수 있습니다. | 숫자만 입력 |
| submit | button | 등록하기 | - | - | - | - |

## 3. 성공 조건

모든 검증을 통과하면 "상품이 등록되었습니다." 문구를 노출한다.

## 4. 실패 조건

필수 입력이 비어 있으면 "필수 입력 항목입니다." 를 노출한다.

## 5. 전제

이 화면은 판매자 계정으로 로그인한 상태에서 동작한다.
비로그인 상태로 접근하면 로그인 화면으로 이동시킨다.

| 이메일 | 비밀번호 |
|---|---|
| seller@test.com | Seller1! |

<!-- page -->

## 화면 흐름 정의

화면 하나만으로는 확인할 수 없는 것이 있다. 가입한 계정으로 실제로 로그인이 되고,
그 로그인으로 상품 등록 화면까지 들어갈 수 있는지는 세 화면을 이어서 밟아야 안다.

| 흐름 ID | 화면 순서 | 이동 방법 |
|---|---|---|
| signup_link_to_product | signup → login → product | 로그인하러 가기 → - |
| signup_state_to_product | signup → login → product | - |

이동 방법을 전이마다 적는다. 화면이 셋이면 옮기는 자리도 둘이고, 둘이 같은 방법일
이유가 없다. `signup_link_to_product` 의 두 전이는 이렇게 갈린다.

- signup → login 은 **완료 화면의 '로그인하러 가기' 를 눌러서** 옮긴다. 화면을 잇는
  요소 자체를 확인하기 위해서다.
- login → product 는 **주소로** 옮긴다('-'). 로그인 직후 화면에는 상품 등록으로 가는
  요소가 없다.

두 흐름은 같은 화면 셋을 밟지만 확인하는 것이 다르다.

- `signup_link_to_product` 는 **화면을 잇는 요소**를 확인한다. 완료 화면의
  '로그인하러 가기' 를 눌러 로그인 화면에 닿는지 본다.
- `signup_state_to_product` 는 **이어진 상태**를 확인한다. 주소로 옮겨서, 가입에 쓴
  계정으로 실제로 로그인이 되고 그 로그인이 상품 등록까지 이어지는지 본다.

하나로 합치면 안 된다. 앞의 잘못(잇는 요소)이 먼저 스텝을 끊어 **뒤의 잘못(이어진
상태)이 가려진다.** 스텝이 끊기면 이후 판정이 무의미하다는 규칙 때문이고 그 규칙은
옳다 — 대신 확인하려는 질문이 둘이면 흐름도 둘이어야 한다.

각 화면의 성공 조건이 그 단계의 확인 기준이다. 중간 화면에서 성공 조건이 충족되지
않으면 그 화면에서 끊긴 것으로 본다 — 마지막 화면이 앞 화면의 잘못을 대신 뒤집어쓰면
고칠 곳을 잘못 짚게 된다.

상품 등록 화면은 로그인한 사용자만 들어갈 수 있으므로, 이 흐름이 마지막 화면에
닿았다는 사실 자체가 앞의 두 화면이 실제로 이어졌다는 증거가 된다.
