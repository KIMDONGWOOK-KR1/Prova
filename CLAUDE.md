# CLAUDE.md

이 저장소에서 작업할 때 지켜야 하는 것. **코드를 고치기 전에 이 파일을 먼저 읽는다.**

현황과 "무엇부터 할까" 는 [`docs/STATUS.md`](docs/STATUS.md) 에 있다.

---

## 실행

README "빨리 돌려보기" 기준이다. PowerShell.

```powershell
# 1. 의존성 (처음 한 번)
uv sync --extra dev
uv run playwright install chromium     # 빠뜨리면 E2E 테스트 232개가 전부 ERROR 다

# 2. 기획서 PDF 생성 (md -> pdf)
uv run python scripts/make_spec_pdf.py

# 3. 테스트 대상 웹앱 띄우기 (별 터미널, 켜 둔 채로 둔다)
uv run uvicorn sut.app:app --port 8100 --reload --reload-dir sut

# 4. 검증
uv run prova run --pdf fixtures/specs/login_spec.pdf --url http://localhost:8100/good --backend mock
uv run prova run --pdf fixtures/specs/login_spec.pdf --url http://localhost:8100/bad  --backend mock

# 5. 리포트 열기
start runs/<run-id>/report.html
```

그 밖에

```powershell
uv run prova serve      # 웹 UI (http://127.0.0.1:7007) — 실행 전에 계획을 보여주고 승인받는다
uv run prova check      # vLLM 연결 + 정형 출력 확인 (GPU 서버가 있을 때)
uv run prova run ... --only <패턴> --headed --slow 500   # 브라우저를 띄우고 천천히 — 눈으로 볼 때
```

`sut/` 를 고쳤으면 **SUT 를 재시작한다.** 안 하면 옛 코드를 상대로 재게 되고 그 결과가
도구의 오탐처럼 보인다. (`prova run` 이 `GET /__build__` 로 확인해 어긋나면 시작하지 않는다.)

---

## 테스트

```powershell
uv run pytest            # 전체
uv run pytest -q         # 짧게
uv run pytest tests/test_rule_expander.py -q      # 파일 하나
uv run pytest tests/test_pipeline_e2e.py -q -x    # 첫 실패에서 멈춤
```

- 현재 기준선: **1117 passed · 105 skipped · 0 failed** (2026-09-23, 커밋 `b5cc299`)
- skip 105개는 **실모델 측정**이다. vLLM 이 없으면 자동으로 빠진다 — 정상이다.
  다만 **다른 모델이 떠 있으면 skip 이 아니라 fail 난다**
- E2E 는 오래 걸린다 (전체 20분쯤). 작업 중에는 관련 파일만 돌리고, 커밋 전에 전체를 돌린다

---

## mock 백엔드

**`--backend mock` 은 명시할 때만 쓴다. 기본값이 아니고, 기본값으로 만들지도 않는다.**

- mock 은 **픽스처 기획서 전용 연습 모드**다. 정답을 이미 아는 백엔드라서
  **배관(파이프라인이 끝까지 도는가)만 증명하고 추출 정확도는 증명하지 않는다**
- 실물 기획서나 외부 사이트에 mock 을 쓰면 안 된다. 의미 있는 결과가 나오지 않는다
- `--backend` 를 빼면 실물 vLLM 을 쓴다. 그게 기본이다
- **측정 결과를 문서에 적을 때는 mock 인지 실모델인지 반드시 함께 적는다.**
  `docs/measurements/` 의 기존 문서들이 전부 그렇게 하고 있다

---

## 설계 원칙 셋

이 저장소에서 내려진 판단은 대부분 이 셋 중 하나로 설명된다.

### 1. 케이스는 코드가 만든다

규칙에서 위반값을 만드는 일은 **순수 함수**(`s2_case_generator/rule_expander.py`)가 한다.
LLM 에게 맡기지 않는다. 판정(`s5_verifier/`)에도 LLM 이 없다.
**LLM 이 사실을 읽는 곳은 S1 하나뿐**이고, 그나마 표에 그대로 적힌 것은 결정적 파서가 읽어
LLM 출력을 교정한다.

> 판정에 LLM 이 없다는 것이 이 도구를 신뢰할 근거다. 그 경계를 넘는 변경은 하지 않는다.

### 2. 못 하는 건 경고로 남긴다

도구가 **확인하지 못한 것**을 통과로도 결함으로도 두지 않는다.

- 기획서에 적혀 있는데 어떤 케이스도 확인하지 않는 것 → `coverage.py` 가 리포트에 항목으로 쓴다
- 기대 문구가 조작 전부터 화면에 있어 그 문구로는 결과를 알 수 없을 때 → `unverifiable` (설계 판단 19)
- 모델이 빠뜨린 요소를 표에서 채웠을 때 → 채운 사실을 경고로 남긴다
- 표를 못 읽었을 때 → 결정적 검증이 꺼졌다는 사실을 경고로 알린다

> **없는 결함을 보고하는 것이 놓치는 것보다 나쁘다.** 개발자의 시간을 쓰고 도구의 신뢰를
> 없앤다. 애매하면 침묵하거나 경고를 남기지, 확신에 찬 FAIL 을 내지 않는다.

### 3. 기획서에 없는 문구는 쓰지 않는다

기대 문구는 **기획서가 적은 것만** 쓴다. 모델이 그럴듯한 안내 문구를 지어내면 그걸로
없는 결함 하나가 생긴다 — 실제로 겪었다 (설계 판단 11).
결정적 파서도 같은 원칙이다: **확신할 수 있을 때만 답하고, 애매하면 `None`/빈 값을 돌려준다.**
틀린 확신이 침묵보다 나쁘다.

---

## 변경할 때

1. **테스트 먼저.** 고치려는 동작을 드러내는 테스트를 먼저 쓴다.
   `tests/` 에 83개 파일이 있으니 비슷한 것을 찾아 흉내 낸다.
   외부 사이트에서 배운 모양은 사이트를 부르지 말고 `set_content`·`page.route` 로 고정한다
   (`tests/test_real_site_shapes.py` 참고)
2. **작은 커밋.** 한 커밋에 한 가지. 커밋 메시지는 기존 것을 보고 따라 쓴다 —
   *무엇을 했는지가 아니라 무엇이 바뀌었는지*를 평서문으로 적는다
   (예: "표 열 제목이 라벨과 조금 달라도 읽고, 못 읽으면 말한다")
3. **`docs/pr/` 형식으로 기록.** 주제 하나가 끝나면 `docs/pr/NN-<주제>.md` 를 쓴다. 형식은:
   - 제목 + 커밋 해시·규모
   - **"이 변경이 답하는 질문"** — 무엇이 문제였나
   - **"무엇을 했나"** — 바뀐 파일과 코드
   - 측정했다면 숫자와 그 출처
4. **[`docs/design-decisions.md`](docs/design-decisions.md) 의 설계 판단을 어기지 않는다.**
   현재 **1~22번**이다. 어겨야 할 이유가 생겼다면 그건 판단을 바꾸는 일이므로,
   코드를 고치기 전에 문서를 고치고 팀에 알린다
   - *(인수인계 시점의 지시는 "1~18" 이었다. 2026-09-23 커밋 `b5cc299` 에서 19~21 이
     추가됐고, 같은 날 22 가 더해졌다 — 앞의 셋은 외부 사이트 관통에서, 22 는 표 경로의 빈 통과에서 배운 것이다.)*
5. 수치를 문서에 적을 때는 **출처 파일과 측정 날짜를 함께** 적는다.
   `docs/measurements/` 의 기존 문서를 본보기로 삼는다

---

## git

- **작업 브랜치는 `Ye-wonnie`.** 모든 커밋은 여기에 한다
- **`main` 에 직접 커밋하지 않는다**
- **`push` 는 사람이 확인한 뒤에만 한다.** 에이전트가 임의로 push 하지 않는다
- `main` 이 움직이면 `git fetch origin && git merge origin/main` 으로 따라간다
  (2026-09-23 에 실제로 하루 사이 커밋 3개가 들어왔다)

---

## 하지 않을 것

- 판정 경로(S5)에 LLM 을 넣는 것
- 기획서에 없는 기대 문구를 코드가 만들어 내는 것
- `--backend mock` 을 기본값으로 만드는 것
- 측정 없이 "개선됐다" 고 문서에 적는 것
- 로드맵이 **"실물 문서를 본 뒤 결정한다"**(표 헤더 넓히기) 또는
  **"일부러 미룬다"**(`screen_name`↔`<title>`)고 적어 둔 것을 그냥 하는 것 —
  미뤄 둔 이유가 오탐 위험이다
