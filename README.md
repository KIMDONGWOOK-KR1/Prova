# Prova

**설계 문서를 읽어 테스트를 스스로 만들고, 실제 브라우저로 실행해 기획과 구현의 불일치를 찾아내는 웹 GUI QA 에이전트.**

팀 소인배 · 2026 하반기 WE-Meet · 의뢰: ㈜WITCHES

---

## 왜 만드는가

개발 현장에서 기획서와 실제 구현이 어긋나는 일이 잦고, 그걸 찾아 고치는 데 시간이 든다.
지금 그 확인은 QA 담당자가 화면기획서를 읽고 손으로 테스트 케이스를 만들어 하나씩 눌러보는 수작업이다.

Prova는 이 작업을 자동화한다. 증명하려는 명제는 하나다 —
**"기획서에 적힌 검증 규칙이 구현에 빠져 있으면 Prova가 그것을 짚어낸다."**

### 지금 동작하는 것

```
<화면>_spec.pdf ──S1──> SpecDocument ──S2──> TestCase[] ──S3+S4──> 브라우저 실행
                        (ScreenSpec[] + Flow[])                        │
                          report.html <──S6── Verdict[] <──S5─────────┘
```

로그인·회원가입·검색·비밀번호 찾기 네 화면을 각각 두 구현에 대해 검증한 결과, 그리고 세 화면을
한 문서에 담고 **화면 사이의 흐름**까지 검증한 결과
(CHEETAH의 로컬 Qwen2.5-7B-AWQ 사용, 2026-08-17 실측):

| 화면 | 대상 | 결과 |
|---|---|---|
| 로그인 | `good` — 기획서를 지킨 구현 | **10/10 PASS** (통과율 100%) |
| 로그인 | `bad` — 검증을 빠뜨린 구현 | **4 PASS / 6 FAIL** — 심은 결함 4종을 모두 지목, 오탐 0건 |
| 회원가입 | `good` | **17/17 PASS** |
| 회원가입 | `bad` | **13 PASS / 4 FAIL** — 심은 결함 4개를 정확히 지목, 오탐 0건 |
| 검색 | `good` | **10/10 PASS** |
| 검색 | `bad` | **6 PASS / 4 FAIL** — 심은 결함 3개를 지목(하나는 두 경로에서), 오탐 0건 |
| 비밀번호 찾기 | `good` | **9/9 PASS** |
| 비밀번호 찾기 | `bad` | **7 PASS / 2 FAIL** — 계정 존재 여부 노출(실제 취약점)을 두 관점에서 지목, 오탐 0건 |
| 통합 문서 (3화면+흐름 2) | `good` | **39/39 PASS** |
| 통합 문서 (3화면+흐름 2) | `bad` | **23 PASS / 16 FAIL** — 화면별 결과가 단일 문서와 동일, 오탐 0건 |
| 검색 (`nolabel` — 아이콘 버튼) | 2차 경로 **끔** | **1 PASS / 9 FAIL** — 라벨이 없어 요소를 찾지 못한다 |
| 검색 (`nolabel` — 아이콘 버튼) | 2차 경로 **켬** (실물 Qwen2.5-VL-3B-AWQ) | **9 PASS / 1 FAIL** — 이미지로 찾아 진행하고, 라벨 결함 1건은 남는다 |
| 로그인 (`slow` — 결과가 400ms 뒤에 렌더) | 대기 **끔** | **3 PASS / 7 FAIL** — 못 기다린 것을 구현 결함으로 단정한다(오탐) |
| 로그인 (`slow` — 결과가 400ms 뒤에 렌더) | 대기 **켬** | **10 PASS / 0 FAIL** — `good` 과 같아진다. 기다린 케이스 7건을 리포트에 남긴다 |
| 로그인 (`spa` — URL 이 안 바뀜) | — | **9 PASS / 1 FAIL** — FAIL 이 맞다. 사유가 '경로 미이동 + 문구 노출 확인' 이라 라우팅 문제임을 바로 안다 |
| 로그인 (`hashed` — id 가 해시) | — | **10 PASS / 0 FAIL** — 라벨 전략이 먼저 통해 영향이 없다 |
| 로그인 (`native` — 브라우저가 제출을 막음) | — | **8 PASS / 2 FAIL** — FAIL 이 맞다. 사유를 '강제하지 않는다' 에서 '다른 방법으로 강제한다' 로 고쳤다 |
| 상품등록 (2화면 문서: 로그인+상품등록) | `good` — 전제(로그인)를 지킨 구현 | **20/20 PASS** |
| 상품등록 (2화면 문서) | `bad` | **12 PASS / 8 FAIL** — 로그인 화면 결함 6 + 심은 결함 2종(가격 숫자 검사 누락 P1, 로그인 가드 누락 P2)을 모두 지목, 오탐 0건 |
| 주문조회 (2화면 문서: 로그인+주문조회) | `good` | **15/15 PASS** |
| 주문조회 (2화면 문서) | `bad` | **7 PASS / 8 FAIL** — 로그인 화면 결함 6 + 심은 결함 2종(오래된순 정렬 O1, 합계 마지막 행 누락 O2)을 모두 지목, 오탐 0건 |

위 네 행은 **CHEETAH 실물 7B 관통 실측**(2026-08-20)이다. 2026-08-19 의 mock
백엔드 E2E(`tests/test_product_e2e.py`·`tests/test_orders_e2e.py`)로 배관과 판정
로직을 먼저 증명한 뒤 실모델로 다시 쟀다. 첫 실측에서 7B 가 주문조회의 표시 전용
요소 4개(목록·텍스트)를 두 번 다 0개 추출해 O1·O2 를 실행조차 못 했다 — 입력·버튼
요소는 옮기면서 표시 요소는 통째로 빠뜨리는 경향이다. 요소 표는 파서가 이미 읽고
있었으므로(누락 경고가 "표에는 4개" 라고 알았다) 대조를 백필로 올려 해결했다:
모델이 빠뜨린 요소를 표에서 직접 채우고, 채운 사실을 경고로 남긴다
(`_backfill_declared_elements`, `tests/test_element_backfill.py`). 자유 서술인
검증 규칙 셀은 옮기지 않는다 — 규칙이 적힌 요소를 백필하면 위반 케이스가
생성되지 않는다는 경고를 따로 낸다.

2차 경로는 **실물 시각 모델(Qwen2.5-VL-3B-AWQ)로 굳은 시험지 50개를 채점했다**
(2026-08-22, 화면 13장 · 있음 40 · 없음 10, `fixtures/iou/`). 그 전까지의 초록불은 정답을
아는 mock 이 낸 것이어서 배관만 증명했고 정확도는 측정되지 않은 상태였다.

| 지표 | 값 | 뜻 |
|---|---|---|
| 탐지 성공률 (IoU≥0.5) | **21/40 = 52.5%** | 명세서 §9 기준 — **미달** |
| 적중률 (중심이 요소 안) | **36/40 = 90.0%** | 실제로 클릭이 되는 비율 |
| 오탐 (없는 것을 찾았다고) | **7/10 = 70.0%** | 전부 신뢰도 0.8 이상 — 신뢰도 관문이 못 거른다 |
| 호출 시간 | 평균 737ms · 최대 1.7s | |

세 숫자를 한 값으로 합치지 않는다. 적중과 IoU 의 차이는 상자 **크기**다(입력란을 라벨까지,
버튼을 여백까지 잡는다) — 클릭에 필요한 것은 위치뿐이라 실행 성패는 적중률이 말하고,
§9 의 IoU 기준으로는 미달이다. 오탐 70% 는 2차 경로의 진짜 위험이다 — 요소가 정말 없는
화면에서 좌표를 내면 탐지 실패가 구현 결함으로 둔갑한다. 좌표 아래 조작 가능 요소를
확인하는 관문(`_require_actionable`)이 빈 공간은 막지만 **다른 입력란 위에 떨어진 오탐은
통과**한다. 전체 표는 `docs/measurements/vlm-iou-qwen-vl-2026-08-22.md`, 해석은 노트 13.

파이프라인 결과가 mock 과 **동일하다**(9 PASS / 1 FAIL, 보정 8건). 즉 이 화면에서는
실물 모델의 정확도 손실이 없었다.

`bad`에서도 구현돼 있는 검증은 PASS로 나온다. 한 리포트 안에
"구현된 규칙은 PASS, 누락된 규칙은 FAIL"이 함께 나오는 것이 판정을 신뢰할 근거다.

회원가입 화면이 이 점을 더 강하게 보여준다. `required` 규칙을 가진 요소가 6개인데
검증이 빠진 것은 약관 동의 하나뿐이고, Prova는 **그 하나만** FAIL로 지목한다.
닉네임도 `min_length`는 PASS, `max_length`만 FAIL이다 — 한 요소의 규칙 중 일부만
구현된 경우를 규칙 단위로 분리해 짚어낸다.

검색 화면은 검증 축이 다르다. 로그인·회원가입은 "규칙을 어긴 값에 에러가 뜨는가"였는데
검색은 "정상 입력에 정해진 결과가 나오는가"다. `bad`에 심은 **대소문자 구분** 결함이
그 차이를 보여준다 — 값에 흠이 없고 구현도 검사를 하는데 **검사 방법이 기획서와 다르다.**
위반값을 만드는 방식으로는 도달할 경로가 없고, 기획서가 입력-결과 짝을 제시해야 잡힌다.

그리고 검색은 결과를 **두 경로로** 확인한다. 화면에 "검색 결과 3건" 문구가 떴는지
보는 경로와, 결과 목록의 항목을 DOM 에서 직접 세는 경로다. 문구 경로는 화면이 건수를
말해 줄 때만 성립하므로, 건수를 문구로 보여주지 않는 화면에서는 아무것도 확인하지 못한
채 초록불이 된다. `bad`의 FAIL 4건 중 2건이 같은 결함(대소문자 구분)을 서로 다른 경로로
잡은 것이다 — 중복이 아니라, 한쪽만으로는 놓칠 수 있는 결함이 있기 때문에 둘을 둔다.

S1 추출 정확도도 골든 데이터와 대조해 확인했다 — 세 화면 **54/54 통과**.
로컬 7B가 `constraints` 키 이름(`min_length`, `require_uppercase`, `same_as` 등)까지
정확히 뽑아내므로, 계획 단계에서 우려했던 "7B가 검증 규칙을 놓칠 위험"은 해소됐다.
Claude API가 필요하지 않다.

---

## 저장소에 있는 다른 도구

`extract_auth_search_json.py`는 이 파이프라인보다 먼저 만들어진 독립 추출 도구다.
**HWPX 파싱과 OCR**을 지원하는데 이 파이프라인에는 없는 능력이라 그대로 둔다.
`feature/figma-extractor` 브랜치에는 Figma 추출기가 있다.
자세한 내용은 [docs/reference/legacy-extractors.md](docs/reference/legacy-extractors.md).

측정 스크립트는 `scripts/` 에 있다. 도구를 재는 것과 도구 자체를 구분해 두려고
`prova` 패키지 안에 넣지 않았다.

| 스크립트 | 재는 것 | GPU |
|---|---|---|
| `probe_s1_robustness.py` | 기획서가 훼손됐을 때 추출이 어떻게 깨지는가 (6종) | 필요 |
| `build_iou_dataset.py` | — 탐지 시험지를 굳힌다 (`fixtures/iou`) | 불필요 |
| `eval_vlm_iou.py` | 화면 이미지에서 요소를 찾는 정확도 (IoU·적중·오탐·속도) | 필요 |

---

## 코드를 처음 보는 사람은

`docs/teaching/overview.html`을 브라우저로 열면 파이프라인 구조와 결과를 그림으로 볼 수 있다.
그다음 `docs/teaching/00-overview.md`부터 번호 순으로 읽으면 각 단계의 설계 판단을 알 수 있다.

---

## 빨리 돌려보기

```powershell
# 1. 의존성
uv sync --extra dev
uv run playwright install chromium

# 2. 기획서 PDF 생성 (md -> pdf)
uv run python scripts/make_spec_pdf.py

# 3. 테스트 대상 웹앱 띄우기 (별 터미널)
uv run uvicorn sut.app:app --port 8100

# 4. 검증 — GPU 없이도 mock 백엔드로 파이프라인 전체가 돈다
uv run prova run --pdf fixtures/specs/login_spec.pdf  --url http://localhost:8100/good --backend mock
uv run prova run --pdf fixtures/specs/login_spec.pdf  --url http://localhost:8100/bad  --backend mock
uv run prova run --pdf fixtures/specs/signup_spec.pdf --url http://localhost:8100/good --backend mock
uv run prova run --pdf fixtures/specs/signup_spec.pdf --url http://localhost:8100/bad  --backend mock
uv run prova run --pdf fixtures/specs/search_spec.pdf --url http://localhost:8100/good --backend mock
uv run prova run --pdf fixtures/specs/search_spec.pdf --url http://localhost:8100/bad  --backend mock

# 5. 리포트 열기
start runs/<run-id>/report.html
```

자연어로 무엇을 볼지 좁힐 수 있다. 무엇을 뺐는지는 리포트에 남는다.

```powershell
uv run prova run --pdf fixtures/specs/login_spec.pdf --url http://localhost:8100/bad                  --request "비밀번호 규칙이 제대로 걸리는지 확인해줘"
```

화면으로 쓰려면 웹 UI 를 연다. 요청을 쓰면 **실행 전에 무엇을 테스트할지 보여주고
승인을 받는다.**

```powershell
uv run prova serve      # http://127.0.0.1:7007
```

실제 로컬 LLM으로 돌리려면 [docs/CHEETAH_SETUP.md](docs/CHEETAH_SETUP.md)를 따라
CHEETAH에 vLLM을 올리고 `--backend` 없이 실행한다.

```powershell
uv run prova check     # vLLM 연결 + 정형 출력 동작 확인
uv run pytest          # 테스트 754개 (vLLM 없으면 정확도 측정 69개는 자동 skip)
```

---

## 구조

```
src/prova/
├── models.py                 데이터 계약 (명세서 §3) — 파이프라인 전체의 타입
├── theme.py                  디자인 토큰 — 웹 UI 와 리포트가 공유하는 단일 출처
├── text_utils.py             PDF 문구 ↔ 화면 문구 비교 규칙
├── llm/                      백엔드 추상화 (base / mock / vllm)
├── s1_spec_extractor/        PDF -> ScreenSpec
│   ├── pdf_parser.py           결정적 추출 (pdfplumber, LLM 없음)
│   └── extractor.py            LLM 구조화 + 표에서 읽은 사실로 교정
├── s2_case_generator/        ScreenSpec -> TestCase[]
│   ├── rule_expander.py        규칙 -> 위반값 + 요소 간 의존 해석 (순수 함수, LLM 없음)
│   ├── precondition.py         전제(로그인) 스텝 생성 (순수 함수, LLM 없음)
│   ├── generator.py            케이스 조립 + 기대 결정 + 요소 유형별 액션
│   ├── selector.py             자연어 요청 -> 실행할 케이스 고르기 (부분집합만)
│   └── coverage.py             기획서에 적혀 있는데 확인하지 않는 것을 찾는다
├── s3_grounder/dom_locator.py  라벨 -> 실제 요소 (selector-first)
├── s4_executor/                Playwright 실행 + 스텝별 증거 수집
├── s5_verifier/                PASS/FAIL 판정
├── s6_report/                  JSON + HTML 리포트
├── nodes.py                  (state) -> state 노드
├── pipeline.py               build_plan (브라우저 없이 S1~S2+선택) + run_pipeline
├── graph.py                  같은 노드를 LangGraph 로 배선
├── server/                   로컬 웹 UI — 계획 확인 -> 실행
│   ├── app.py                  /api/plan (브라우저 없음) · /api/run (승인 목록) · /api/runs
│   ├── runner.py               워커 스레드 하나, 동시 실행 하나
│   └── static/                 작업 콘솔 (index.html · app.css · app.js, CDN 없음)
└── cli.py                    prova run / prova serve / prova check

sut/                          테스트 대상 미니 웹앱 — 로그인·회원가입·검색 (good / bad)
fixtures/specs/               화면기획서 md / pdf / 정답(golden) — 화면마다 한 벌
├── multi_spec.md               통합 문서 — 화면별 md 에서 조립한다 (직접 고치지 않는다)
└── _multi_*.md                 조립용 조각 ('_' 로 시작하면 PDF 로 만들지 않는다)
scripts/                      기획서 PDF 생성 · 통합 문서 조립 · 데모 자료 생성
docs/
├── specs/                    Prova_서비스명세서.md (개발 기준 문서)
├── reference/                계획서·주제개요서·멘토링보고서
├── teaching/                 티칭 노트 17개 + overview.html (그림 자료)
├── CHEETAH_SETUP.md          GPU 서버 vLLM 세팅 절차
└── README.md                 문서 안내
```

---

## 설계 판단 (명세서와 다르게 한 것)

명세서([`docs/specs/Prova_서비스명세서.md`](docs/specs/Prova_서비스명세서.md))를
구현하면서 열네 군데를 바꿨다. 각각 이유가 있다.

절이 길어서 목차를 둔다. 급하면 **10·11·12·13·14** 를 보면 된다 — 도구가 스스로
없는 결함을 보고하던 것(10~13)과, 반대로 있는 결함을 덮을 뻔한 것(14)을 막은
이야기다. 이 프로젝트에서 가장 배울 것이 많다.

| | 무엇을 다르게 했나 |
|---|---|
| 1 | [negative 케이스는 규칙을 하나씩만 위반시킨다](#1-negative-케이스는-규칙을-하나씩만-위반시킨다) |
| 2 | [`rule_expander`를 LLM 밖 순수 함수로 뒀다](#2-rule_expander를-llm-밖-순수-함수로-뒀다) |
| 3 | [S1을 결정적 추출 + LLM 구조화 2단계로 나눴다](#3-s1을-결정적-추출--llm-구조화-2단계로-나눴다) |
| 4 | [`constraints`에 요소 간 규칙(`same_as`)을 넣었다](#4-constraints에-요소-간-규칙same_as을-넣었다) |
| 5 | [규칙으로 표현할 수 없는 검증을 `scenarios`로 따로 뒀다](#5-규칙으로-표현할-수-없는-검증을-scenarios로-따로-뒀다) |
| 6 | [건수는 문구로 확인하지 않고 DOM 을 센다 — 계약은 그대로 두고 경로를 늘렸다](#6-건수는-문구로-확인하지-않고-dom-을-센다--계약은-그대로-두고-경로를-늘렸다) |
| 7 | [화면 사이의 결함을 잡는 경로를 뒀다 — 그리고 오진 가능성을 먼저 막았다](#7-화면-사이의-결함을-잡는-경로를-뒀다--그리고-오진-가능성을-먼저-막았다) |
| 8 | [기획서의 '유형' 열을 구현과 대조한다](#8-기획서의-유형-열을-구현과-대조한다) |
| 9 | [확인하지 않은 것을 리포트에 쓴다](#9-확인하지-않은-것을-리포트에-쓴다) |
| 10 | [2차 경로를 넣으면서 '보정'이 결함을 못 지우게 막았다](#10-2차-경로를-넣으면서-보정이-결함을-못-지우게-막았다) |
| 11 | [기획서 본문에 없는 문구를 요구사항으로 쓰지 않는다](#11-기획서-본문에-없는-문구를-요구사항으로-쓰지-않는다) |
| 12 | [화면이 기대 상태에 도달하기를 기다린다 — 그리고 기다린 사실을 남긴다](#12-화면이-기대-상태에-도달하기를-기다린다--그리고-기다린-사실을-남긴다) |
| 13 | ['없어야 할 것이 없는가' 를 확인하는 경로를 뒀다](#13-없어야-할-것이-없는가-를-확인하는-경로를-뒀다) |
| 14 | [자연어 요청을 받으면서, 그 해석이 결함을 덮지 못하게 막았다](#14-자연어-요청을-받으면서-그-해석이-결함을-덮지-못하게-막았다) |

### 1. negative 케이스는 규칙을 하나씩만 위반시킨다

명세서 예시 `signup-pw-no-upper-002`는 `require_uppercase`와 `require_special`을
동시에 위반한다. 그러면 FAIL이 떴을 때 **어느 규칙이 미구현인지 분리되지 않는다.**

```
require_uppercase 위반 -> "a1!aaaaa"   (대문자만 없음, 나머지 규칙 충족)
require_special   위반 -> "Aa1aaaaa"   (특수문자만 없음)
min_length        위반 -> "Aa1!aaa"    (길이만 부족)
```

리포트에서 규칙과 실패가 1:1로 연결되어, 개발자가 어느 검증 로직을 추가해야 하는지 바로 안다.

### 2. `rule_expander`를 LLM 밖 순수 함수로 뒀다

'대문자 1자 이상' 규칙에서 '대문자가 없는 값'을 만드는 데는 추론이 필요하지 않다.
코드로 짜면 결정적이고, 공짜이고, **생성값이 정말 그 규칙만 위반하는지 단위 테스트로
보증**할 수 있다. LLM 출력에는 그런 보증을 걸 수 없다.

LLM은 판단이 필요한 곳에만 쓴다 — S1의 자연어→스키마 매핑, S2의 케이스 제목.

### 3. S1을 결정적 추출 + LLM 구조화 2단계로 나눴다

명세서 §10-2는 PDF 추출을 🔴 API 후보로 봤다. 그건 이미지·자유서술이 섞인 실무
기획서 전제다. 텍스트 레이어가 있는 PDF는 pdfplumber가 표와 글자를 정확히 읽으므로,
LLM에 남는 일은 텍스트→JSON 구조화뿐이고 로컬 7B로 충분하다.
WITCHES 실물 PDF가 오면 `pdf_parser.py`만 교체하면 된다.

### 4. `constraints`에 요소 간 규칙(`same_as`)을 넣었다

명세서의 `constraints`는 값 하나만 보고 판정되는 규칙만 담는다. 그런데 회원가입의
'비밀번호 확인'은 자기 규칙이 없고 **다른 요소와 같아야 한다**는 것이 규칙이다.

이 규칙 하나가 값 생성 방식을 바꿨다. 요소 단위 함수로는 값을 정할 수 없어
화면 단위 해석(`resolve_values`)이 필요해졌고, 의존 순서대로 풀어야 한다.

더 미묘한 문제는 '한 케이스는 한 규칙만 위반한다'는 계약과 부딪히는 지점이다.
비밀번호에 위반값을 넣으면 비밀번호 확인 값도 함께 어긋나 두 규칙이 동시에 깨진다.

→ 위반값을 먼저 고정하고 **그것에 의존하는 값들을 다시 계산한다**
(`resolve_values(inputs, overrides=...)`). 비밀번호 확인은 위반값과 같은 값이 되어,
깨지는 규칙은 여전히 비밀번호의 것 하나다.

같은 이유로 체크박스·선택 요소에는 문자 규칙을 전개하지 않는다. 사용자가 문자를
직접 입력하지 않으므로 '8자 미달인 체크 상태' 같은 입력은 만들 수 없고, 억지로
만들면 실행이 조작 오류로 실패해 판정이 뭉개진다. 이 요소들에서 검증할 수 있는
것은 '선택/체크하지 않았을 때'뿐이다.

### 5. 규칙으로 표현할 수 없는 검증을 `scenarios`로 따로 뒀다

검색 화면에서 드러났다. 로그인·회원가입의 검증은 모두 **'값 자체가 규칙을 어겼는가'**여서
규칙에서 위반값을 만들어낼 수 있었다. 그게 `rule_expander`의 존재 이유다.

검색은 **'정상 입력에 정해진 결과가 나오는가'**다. `notebook`을 검색하면 3건이 나와야
한다는 것은 값의 흠이 아니라 시스템 상태에 대한 기대이고, 규칙이 없으니 위반값도 만들 수
없다.

→ `ScreenSpec.scenarios`를 추가했다. 기획서가 입력-결과 짝을 직접 제시하는 경로이고,
`rule_expander`를 거치지 않는다. 케이스 유형은 `positive`다 — 규칙을 어긴 값이 아니므로
`negative`로 두면 S5의 판정이 뒤집혀 '에러가 떠야 PASS'가 된다.

`Scenario`가 담는 것은 `given`(입력값)과 `expect_text`(노출돼야 하는 문구) 둘뿐이다.
`Expectation` 전체를 담으면 표현력은 늘지만 S1이 `type` 열거값까지 골라야 하고, 로컬 7B에
선택지를 늘리면 추출이 흔들린다. 건수를 DOM에서 직접 세야 할 때가 오면 **필드를 추가**한다
— `expect_text`의 의미를 넓혀 재해석하지 않는다. 필드 이름이 할 수 있는 일을 말해야 한다.

이 확장이 잡아내는 결함의 종류가 새롭다. 검색 `bad`의 **대소문자 구분**은 값에 흠이 없고
구현도 검사를 한다 — 다만 기획서와 다른 방법으로 한다. 위반값 생성으로는 도달할 경로가 없다.

### 6. 건수는 문구로 확인하지 않고 DOM 을 센다 — 계약은 그대로 두고 경로를 늘렸다

위 문단이 예고한 그 확장이다. 실제로 필드를 추가했다(`Expectation.count`,
`Scenario.expect_count`, `ExpectedType`에 `result_count`).

문구 비교의 한계가 분명했다. "검색 결과 3건"을 대조하는 방식은 **화면이 건수를 말해 줄
때만** 성립한다. 목록만 렌더하고 건수를 안 찍는 화면에서는 그 케이스가 아무것도 확인하지
못한 채 초록불이 된다. 확인하지 않은 것과 확인해서 통과한 것은 다르다.

세려면 여러 개를 봐야 하는데, S3는 '정확히 1개만 확정한다'가 계약이었다. 그 계약이
지금까지 판정을 신뢰할 근거였으므로 느슨하게 만들면 기존 세 화면의 근거가 함께 약해진다.

→ **계약을 건드리지 않고 경로를 하나 더 뒀다.** `ground()`는 그대로 두고 `count_items()`를
따로 뒀다. 그리고 새 경로에서도 계약은 유지된다 — **컨테이너는 여전히 정확히 1개여야
하고**, 세는 것은 그 안의 항목이다. '어디를 셀 것인가'는 모호하지 않고, 모호한 것은 '그
안에 몇 개인가'뿐이며 그건 세면 답이 나온다.

문구 케이스와 건수 케이스를 합치지 않은 것도 판단이다. 합치면 FAIL 하나가 두 가지를
뜻하게 된다 — 문구가 틀렸는지, 렌더된 개수가 틀렸는지. 고칠 곳이 다르므로 판정도 나눈다
(설계 판단 1과 같은 원칙이다).

가장 조심한 지점은 **0건이다.** 결과가 없을 때 목록을 렌더하지 않는 것은 정상 구현이므로,
목록의 부재를 탐지 실패로 처리하면 완벽한 화면에서 오탐이 난다. 그래서 `CollectionCount`가
`status`와 `count`를 나눠 담는다 — '못 찾았다'와 '0개다'는 숫자로는 둘 다 0이지만 다른
사실이다. 기대값이 0일 때만 부재가 확인이 된다.

### 7. 화면 사이의 결함을 잡는 경로를 뒀다 — 그리고 오진 가능성을 먼저 막았다

명세서는 화면 단위 검증만 규정한다. 그런데 화면 단위에는 **구조적 사각지대**가 있다.
회원가입이 입력을 다 검증하고 완료 화면까지 보여주면서 계정을 등록하지 않으면,
회원가입 케이스 14건과 로그인 케이스 7건이 **전부 통과한다.** 어느 화면도 결함이 아니고,
결함은 두 화면 사이에 있다. 실무에서 troubles 로 이어지는 불일치가 대개 이 모양이다.

→ `Flow`(화면 순서)와 `SpecDocument`(한 문서에 여러 화면)를 추가하고, 화면을 이어서
밟는 케이스를 만든다.

**이 기능은 처음에 권하지 않았다.** 이유가 하나였다 — 로그인→검색 흐름이 실패했을 때
로그인이 깨진 건지 검색이 깨진 건지 구분되지 않는다. 앞 화면이 깨지면 뒤 화면의 결과는
무의미해지는데 리포트는 마지막 화면 이름을 달고 FAIL 을 보고한다. **지목이 틀린 FAIL 은
오탐보다 나쁘다** — 개발자가 엉뚱한 코드를 뒤지고 도구를 신뢰하지 않게 된다.

그래서 기능보다 오진 방지를 먼저 만들었다. 화면을 넘길 때마다 `assert` 스텝으로 그
화면의 성공 조건이 충족됐는지 확인한다. 앞 화면에서 끊기면 그 스텝의 실패로 기록되고,
**이미 있던 규칙**("스텝이 끊기면 그 원인이 케이스 실패 원인")이 그 화면을 지목한다.
그 규칙은 요소 미탐지를 위해 만든 것인데 흐름의 중간 끊김에 그대로 맞았다.

값은 **라벨로 이어진다.** 가입에 쓴 이메일로 로그인해야 하므로, 앞 화면에 넣은 값을 뒤
화면의 같은 라벨에 다시 넣는다. `element_id`가 아니라 라벨을 쓰는 이유는 화면이 다르면
ID 도 다를 수 있지만 사용자가 보는 라벨은 같기 때문이다. 이어받은 값의 `same_as` 의존값도
다시 계산한다 — 설계 판단 4에서 만든 `resolve_values`가 그대로 쓰인다.

리포트에서 **흐름을 화면 칸에 넣지 않는다.** 넣어 보니 로그인이 '실패 5' 로 표시됐는데
로그인 자체의 결함은 4개였다. 다섯 번째는 화면 사이의 결함이고 고칠 곳이 다르다.
같은 표에 행만 구분해 두면 '화면은 다 통과인데 흐름만 실패' 라는 모양이 한눈에 보이고,
그 모양이 곧 진단이다.

화면 경계는 **페이지 단위로만** 가른다. 페이지 안까지 쪼개려면 `ParsedPage`의 구조를
바꿔야 하고 그러면 이미 검증된 세 화면의 추출 경로가 전부 영향권에 든다. 못 가르는
경우(한 페이지에 개요 표가 둘)는 경고로 알린다 — 앞 화면에 몰아 넣으면 두 화면의 검증이
모두 조용히 어긋난다.

**도구가 대신 해 주는 일은 검증되지 않는다.** 흐름의 두 번째 화면에 주소로 들어가면
편하지만, 그러면 **화면을 잇는 요소가 검증에서 빠진다** — 완료 화면의 '로그인하러 가기'
링크가 엉뚱한 곳을 가리켜도 도구가 스스로 주소를 치고 들어가므로 흐름은 통과한다.
그래서 흐름 표에 `이동 방법` 열을 두고, 그 값이 있으면 그 요소를 눌러서 옮긴 뒤 다음
화면의 경로에 도착했는지 확인한다.

같은 화면 쌍을 흐름 둘이 밟는 것이 자연스럽다. 확인하는 것이 다르기 때문이다 —
하나는 **화면을 잇는 요소**, 하나는 **이어진 상태**다. 하나로 합치면 앞의 결함이 먼저
스텝을 끊어 뒤의 결함이 가려진다.

### 8. 기획서의 '유형' 열을 구현과 대조한다

'유형' 열은 지금까지 **우리 액션을 고르는 데만** 쓰였다(체크박스면 `check()`). 구현이 그
유형을 지켰는지는 아무도 확인하지 않았고, 그래서 기획서가 '링크' 라고 적은 것을 버튼으로
구현해도 통과했다 — S3 가 텍스트 일치로 찾아내 눌러 주기 때문이다. **기획서에 적혀
있는데 아무도 확인하지 않는 항목이 있으면 그건 도구의 구멍이다.**

→ 탐지 뒤에 선언된 role 로 다시 찾아 본다. 못 찾으면 `SpecTypeMismatch`다.
`GroundingError`와 나눠 둔 이유: 이건 탐지 실패가 아니라 기획-구현 불일치이고, 같은
실패로 묶으면 개발자가 라벨을 고치려 든다. 고쳐야 할 것은 요소의 종류다.

**버튼과 링크만 대조한다.** `input[type=password]`는 ARIA에서 textbox role을 갖지
않으므로 입력란까지 넓히면 비밀번호 칸마다 불일치가 떠서 전부 오탐이 된다. 확인할 수
없는 것을 확인한다고 주장하지 않는 것이 이 도구에서 지켜야 하는 선이다.

### 9. 확인하지 않은 것을 리포트에 쓴다

이 도구가 낼 수 있는 가장 위험한 리포트는 FAIL 이 아니다. **"33/33 통과" 인데 실은
기획서의 일부를 확인하지 않은 리포트**다. FAIL 은 눈에 띄지만 '확인하지 않음' 은 아무
흔적도 남기지 않으므로, 읽는 사람은 다 확인됐다고 믿고 넘어간다.

→ 기획서가 적어 둔 **문구**를 모두 모으고, 그중 어떤 케이스의 기대값에도 들어가지 않는
것을 리포트에 항목으로 쓴다(`s2_case_generator/coverage.py`). 경고 목록과 섞지 않는다 —
이건 기획서의 결함도 구현의 결함도 아니라 **검증 범위**에 대한 사실이고, 고칠 곳이
기획서일 수도 도구일 수도 있다. 통과율 바로 옆에 놓아 그 숫자의 범위를 알 수 있게 한다.

이 검출기가 **가장 처음 만든 로그인 화면**에서 구멍을 찾았다. §4 실패 조건의
"등록되지 않은 계정 → '이메일 또는 비밀번호가 올바르지 않습니다.'" 를 확인하는 케이스가
하나도 없었는데, 그동안 리포트는 '로그인 7/7 통과' 라고 보고했다. 그 상황은 규칙을 다
지킨 값으로만 만들 수 있어서 위반값 생성으로는 도달할 수 없었다 — 검색 화면의
`scenarios` 와 같은 성질이다.

**고친 곳이 코드가 아니라 기획서다.** 기획서에 예시 동작을 추가하자 기존 기계가 그대로
검증했고, 그러자 `bad` 의 B3 결함(기획서와 다른 에러 문구)이 처음으로 자기 케이스를
갖게 됐다. 그전까지 B3 는 다른 FAIL 의 곁증거로만 나타났다.

같은 물음("이 필드는 검증되는가, 그냥 쓰이는가?")이 구멍 둘을 더 찾았다.
`options`(선택 목록)는 정상 케이스가 넣을 값을 고르는 데만 쓰였다 — 첫 항목을 골라 넣고
그것이 있으면 통과하므로, 화면에 '지인 추천' 이 빠져 있어도 아무 일도 일어나지 않았다.
선택 요소마다 케이스 한 건을 두어 대조한다. 요소를 쓰는 케이스마다 대조하면 결함 하나가
FAIL 16건이 되어 리포트가 규모를 왜곡한다.

`placeholder`는 반대 방향의 구멍이었다 — **기획서가 아예 적지 않았다.** 필드는 처음부터
있었지만 S3의 탐지 폴백으로만 쓰였다. 안내 문구는 사용자가 가장 먼저 읽는 글자인데
기획서 관할 밖이어서, '아이디를 입력하세요'로 바뀌어도 통과했다. 기획서 요소 표에
`안내 문구` 열을 추가하고 화면당 케이스 한 건으로 대조한다. **필드가 있다는 것과 그 값이
검증된다는 것은 별개다.**

그 열을 더하자 표가 넓어져 회원가입 요소 표의 마지막 행이 **다음 페이지로 밀렸다.**
PDF는 표가 페이지를 넘어가면 헤더를 다시 그리고, pdfplumber는 그것을 별개의 표로 읽는다.
`_element_table()`이 첫 표만 돌려줘 그 행이 사라졌다 — 행 누락을 막으려고 만든 장치가
같은 이유로 무력해진 것이다. 헤더가 같은 표를 이어 붙이도록 고쳤다. 내가 만든 버그가
아니라 요소가 많은 실물 기획서에서 먼저 터질 버그였다.

### 10. 2차 경로를 넣으면서 '보정'이 결함을 못 지우게 막았다

접근성 속성으로 못 찾은 요소를 화면 이미지로 찾는 경로(VLM)를 붙였다. 실물 화면의 아이콘
버튼은 라벨도 접근 가능한 이름도 없어서 S3의 네 전략이 모두 막힌다.

그런데 이 경로에는 **다른 기능에 없는 위험**이 있다. 좌표 클릭은 빈 공간을 눌러도
Playwright가 예외를 내지 않는다. 폼이 제출되지 않고, 에러 문구도 결과도 나오지 않고,
S5는 그것을 "기획서가 지정한 에러가 안 뜬다" 로 읽는다. 실제로 어림잡은 좌표로 돌렸다가
**없는 결함 7건을 보고했다.** 테스트는 통과했다 — `healed > 0` 만 봤기 때문이다.

→ 세 가지를 뒀다. (1) 누르기 전에 `document.elementFromPoint`로 그 자리에 조작 가능한
요소가 있는지 확인하고 없으면 누르지 않고 `element_not_found`로 되돌린다. (2) `healed`는
실제로 진행된(`status == "ok"`) 스텝만 센다 — 시도만 하고 막힌 것을 healed로 세면 리포트가
"이미지로 찾아 진행했다" 고 말하는데 아무것도 진행되지 않은 상태가 된다. (3) 수용 기준을
"보정하면 판정이 `good`과 같아진다"로 바꿨다. `nolabel` 변형은 검증 로직이 `good`과
완전히 같으므로 이 기준이 성립하고, 좌표가 어긋난 순간 테스트가 터진다.

**탐지 실패와 구현 결함을 섞지 않는 것이 핵심이다.** 개발자가 할 일이 다르다 —
`element_not_found`는 "도구를 고쳐라", `assertion_mismatch`는 "구현을 고쳐라" 다. 확신할
수 없으면 탐지 실패로 두는 쪽이 옳다.

그리고 **보정이 결함을 메우게 두지 않았다.** '기획서의 라벨로 요소를 지목할 수 없다' 는
것 자체가 기획-구현 불일치이면서 접근성 결함이다. 보정으로 케이스가 통과하더라도 그 사실은
전용 케이스(`labels_findable`)로 FAIL에 남고, 그 케이스는 보정을 쓰지 않는다. 리포트에도
'화면 이미지로 찾아 진행한 케이스' 구획을 따로 둬서 통과율만 보는 사람도 그 PASS가 어떻게
나온 것인지 알 수 있게 했다.

모델의 역할은 최대한 좁혔다 — `locate(image, target) -> bbox` 하나다. 판정은 여전히 S5의
결정적 규칙이 한다. 경계를 지키면 모델이 틀리는 방식이 "좌표가 어긋난다" 하나로 제한되고,
그 하나는 검사할 수 있다.

실물 모델로 재면서 구멍이 하나 더 드러났다. `health()` 가 `/models` 응답만 보고 **우리
모델 이름이 그 목록에 있는지는 확인하지 않았다.** 서버를 `--served-model-name qwen-vl`
로 띄웠는데 클라이언트가 기본 이름으로 요청하니 `/models` 는 200, `chat/completions` 는
404 였다. 그래서 실행은 정상 기동하고 로그에 '2차 경로: ...' 가 찍히는데 `locate()` 마다
실패하고, 호출부가 그것을 탐지 실패로 되돌려 **리포트가 보정을 끈 실행과 똑같아졌다.**
'서버가 살아 있다' 는 '내 요청이 통한다' 가 아니다 — 기동 시점에 알 수 있는 사실이므로
그때 끊고, `--vlm-model` 로 이름을 맞출 수 있게 했다.

이 구멍은 mock 테스트 26건이 모두 통과하는 동안 그대로 있었다. mock 은 서버 이름 같은
것을 다루지 않는다. **실물로 한 번 돌리는 것이 mock 테스트를 늘리는 것보다 나은 순간이
있다.**

자세한 내용은 [`docs/teaching/13-vlm-fallback.md`](docs/teaching/13-vlm-fallback.md).

### 11. 기획서 본문에 없는 문구를 요구사항으로 쓰지 않는다

픽스처 기획서 4개는 전부 우리가 마크다운으로 써서 스크립트로 PDF 로 만든 것이다. 7열이
정확한 순서로, 정확한 헤더 이름으로, 표 형태로 있다. **S1 의 결정적 표 독해 여덟 개가
전부 그 모양을 전제한다.** 그래서 지금까지의 '오탐 0건' 에는 조건이 숨어 있었다 —
*우리가 만든 기획서에서는.*

기획서를 일부러 훼손해 재 봤다(`scripts/probe_s1_robustness.py`, 6종: 열 이름 변경 ·
열 누락 · 산문 서술 · 셀 병합 · 표 이미지화 · 절 번호 없음). 재는 것은 '깨지는가' 가
아니라 **'어떻게 깨지는가'** 다 — 시끄럽게 깨지는 도구는 쓸 수 있고 조용히 깨지는 도구는
해롭다.

`안내 문구` 열을 지운 문서에서 **7B 가 `'이메일 입력'` 을 지어냈다.** 문서에 그런 말이
한 글자도 없다. 그러자 도구는 안내 문구 대조 케이스를 만들어 **올바른 구현을 FAIL 로
보고했다** — 없는 결함을 자신 있게, 경고 하나 없이.

→ 두 가지를 뒀다. (1) `_drop_invented_strings` 가 `placeholder`·`error_message`·
`required_message` 를 기획서 본문과 대조해, 없는 문구는 버리고 경고를 남긴다. '표에서
읽은 값만 신뢰' 가 아니라 **'본문에 그 문자열이 있는가'** 로 판단한다 — 표든 산문이든
통하고, 실측에서 7B 는 산문에 적힌 안내 문구를 정확히 읽어냈다(버릴 이유가 없는 정보다).
(2) 요소 표를 아예 못 찾으면 그 사실을 경고한다. 그때 결정적 독해 여덟 개가 동시에
무력해지고 `_apply_declared_*` 들은 조용히 반환하는데(`if not declared: return`), 그
사실이 어디에도 남지 않았다.

**지우는 장치를 만들 때는 지우면 안 되는 것부터 쟀다.** 픽스처 3종의 정답 문구 19개가
전부 본문에서 찾아지는 것을 구현 **전에** 확인했고(공백 정규화로 충분), 그 성질을
테스트로 못 박았다. 순서가 반대면 조용한 검증 상실을 만든다.

문구를 버린 위반 케이스는 `error_shown` 으로 격하된다 — '에러가 떴고 이동하지 않았다'
만 확인한다. **검증이 약해지는 것과 거짓이 되는 것은 다르다.**

부수적으로 알게 된 것: 7B 는 표가 없는 산문 기획서에서도 요소 3개와 규칙 4개를 정확히
뽑아냈고, 셀 병합과 절 번호 제거는 결정적 독해에 영향이 없었다. 깨지는 것은 **표 자체를
못 찾을 때** 하나다.

자세한 내용은
[`docs/teaching/14-real-spec-robustness.md`](docs/teaching/14-real-spec-robustness.md).

### 12. 화면이 기대 상태에 도달하기를 기다린다 — 그리고 기다린 사실을 남긴다

기획서를 우리가 만든 것이 문제였던 것처럼, **검증 대상 웹앱도 우리가 만들었다.** `sut/` 는
동기식 폼 POST 라 응답 HTML 에 결과가 이미 들어 있다. 그래서 스텝이 끝나면 곧바로 DOM 을
읽고 판정해도 통했다. 실물 웹앱은 대개 제출하면 fetch 를 보내고 응답이 오면 화면을 갈아
끼운다 — 그 사이 화면에는 아무것도 없다.

검증 로직이 `good` 과 한 줄도 다르지 않고 **렌더 시점만 400ms 늦춘** `slow` 변형을 만들어
재 봤다(`nolabel` 과 같은 원칙 — 변수를 하나만 바꾼다). 결과는 **오탐 7건**이고, 사유가
이랬다.

```
기획서에 적힌 'password' 의 'require_uppercase' 검증 규칙이 구현에서 확인되지
않았습니다. (에러가 전혀 노출되지 않음 — 구현이 이 규칙을 강제하지 않는다)
```

구현은 그 규칙을 강제한다. **도구가 못 기다린 것을 구현 결함으로 단정했다.** 좌표가 어긋나
없는 결함 7건을 보고한 것(설계 판단 10)과, 모델이 요구사항을 발명해 오탐을 만든 것(설계
판단 11)과 원인은 다르지만 모양이 같다.

→ 판정이 통과할 때까지, 상한(`settle_timeout_ms`, 기본 2초)까지 기다린 뒤 판정한다.
재시도는 S4 가 아니라 오케스트레이션(`nodes.py`)에 뒀다 — S4 는 실행하고 S5 는 순수 함수로
판정하는 경계를 지키면서 '언제 판정할지' 만 밖에서 정한다.

**'DOM 이 안정될 때까지' 는 시도했다가 버렸다.** 판정과 독립적이라 깔끔하지만 성립하지
않는다 — 400ms 뒤에 렌더하는 화면은 그 전 250ms 동안 아무 변화가 없고, 그 정적을 '안정됐다'
로 읽는다. **아직 시작하지 않은 것과 끝난 것을 구분할 수 없다.**

대기는 FAIL 을 PASS 로만 바꾼다. 그 방향성이 결함을 덮지 않게 셋을 지켰다. (1) 상한을 넘으면
마지막 판정을 그대로 보고한다. (2) 기다린 시간을 근거와 리포트에 남긴다 — '이 화면이 결과를
비동기로 렌더한다' 는 사실이고, `healed` 를 남기는 것과 같은 이유다. (3) 스텝이 끊긴 케이스는
기다리지 않는다.

수용 기준을 셋 뒀는데 하나가 특히 중요하다 — **`settle_timeout_ms=0` 으로 끄면 오탐이
돌아오는지 확인한다.** 그게 없으면 `slow` 가 통과하는 이유가 대기 때문인지 다른 이유인지
알 수 없다.

비용은 테스트 전체가 282초 → 445초다. 줄이지 않았다 — **느린 것은 참을 수 있고 틀린 것은
참을 수 없다.**

같은 방식으로 실물 마크업 세 가지를 더 쟀다. `spa`(성공해도 URL 이 안 바뀜)는 **고칠 것이
없었다** — FAIL 이 맞고, 사유가 '경로 미이동; 문구 노출 확인' 이라 개발자가 로그인 로직은
되고 라우팅이 문제라는 것을 바로 안다. 미탐을 막으려고 검사마다 결과를 남기게 한 설계가
진단 품질로 돌아온 것이다. `hashed`(id 가 CSS-in-JS 해시)도 **영향이 없었다** — 라벨 전략이
먼저 통하므로 `element_id` 로 만드는 selector 가 필요하지 않다. 가정이 맞았지만 재 봤으니
이제 아는 것이고, 테스트로 남겼다.

`native`(`novalidate` 를 떼고 `required` 를 붙임)에서 **사유가 사실과 반대였다.** 브라우저가
제출을 막아 앱의 에러 문구가 화면에 없는데, 도구는 '구현이 이 규칙을 강제하지 않는다' 고
단정했다. 구현은 강제한다 — 오히려 더 이르게 막는다. 검색 화면의 대소문자 결함과 같은
모양이다: **검사 자체는 하는데 검사 방법이 기획서와 다르다.** `form.checkValidity()` 로
관찰해 사유를 갈랐고, 판정(FAIL)은 그대로 뒀다 — 기획서가 문구를 노출한다고 적었으므로
불일치는 실재한다. 사유에 고치는 방법을 둘 적었다: 문구를 직접 노출하거나 **기획서를
고치는 것.**

자세한 내용은
[`docs/teaching/15-async-rendering.md`](docs/teaching/15-async-rendering.md).

### 13. '없어야 할 것이 없는가' 를 확인하는 경로를 뒀다

1차 범위의 네 번째 화면(비밀번호 찾기)을 붙이면서 드러난 것이다. 입력 검증은 예상대로
로그인의 부분집합이었는데, **이 화면에는 지금까지 없던 방향의 요구사항이 있었다.**

실물 서비스의 비밀번호 찾기는 계정 존재 여부를 알려주지 않는다. 등록 여부에 따라 다른
문구를 노출하면 공격자가 이메일 목록을 넣어 어떤 계정이 가입되어 있는지 확인할 수 있다
(account enumeration). 로그인이 '이메일 또는 비밀번호가 올바르지 않습니다' 로 뭉뚱그려
답하는 것과 같은 이유다.

그런데 이 도구의 판정 유형 여섯 개가 **전부 '있어야 할 것이 있는가'** 였다. 반대 방향이
하나도 없었다.

→ `Scenario.expect_absent` 와 판정 유형 `text_absent` 를 추가했다. 기획서 예시 표에
`노출되면 안 되는 문구` 열을 두고 결정적으로 읽는다.

**`text_visible` 로 재해석할 수 없다.** '재설정 메일을 보냈습니다' 와 '등록되지 않은
이메일입니다' 가 **함께** 떠 있는 화면이 가능하고, 그 화면은 요구를 어겼는데 성공 문구가
있으므로 통과한다. 있어야 할 것과 없어야 할 것은 다른 확인이므로 필드도 판정도 다르다
(`Scenario` 의 문서가 예고한 '필드를 추가한다' 를 따랐다).

열을 찾는 방법도 달라야 했다. 건수 열은 **값의 모양**(전부 정수)으로 찾는데, 금지 문구는
값이 문구라서 기대 문구 열과 모양이 같다. 그래서 헤더로 찾는다. **두 열이 섞이면 성공
문구를 금지 문구로 읽어 올바른 구현이 FAIL 이 되므로**, 헤더 판별을 테스트로 못 박았다.

한 결함이 FAIL 2건이 되는데 중복이 아니다 — 구현이 성공 문구를 아예 안 띄우면 문구 경로가
잡고, 띄우면서 금지 문구도 함께 띄우면 금지 경로만 잡는다. 사유도 다르다: **'기능이 안
된다' 와 '알려주면 안 되는 것을 알려준다' 는 고칠 곳이 다르다.**

필드를 추가하면서 조용히 버려지는 곳이 하나 있었다 — `_apply_declared_scenarios` 의 정규화
목록이 필드 이름을 손으로 적어 둬서, 표에서 읽은 `expect_absent` 가 사라지고 금지 문구
케이스가 아예 생성되지 않았다. 리포트는 정상으로 보였다. 키 목록을 한 곳에 모으고 주석으로
못 박았다.

자세한 내용은
[`docs/teaching/16-forbidden-text.md`](docs/teaching/16-forbidden-text.md).

#### 뒤에 드러난 것 — 대기와 금지 문구가 함께 구멍을 만들었다

설계 판단 12(비동기 대기)와 13(금지 문구)은 **각각은 옳은데 함께 두면 미탐**이었다.
대기는 첫 판정이 PASS 면 멈추는데, 금지 문구는 통과의 뜻이 반대다 — 누출 문구가 400ms
뒤에 나타나면 첫 판정에서 통과하고 그대로 확정된다.

12를 만들 때 있던 판정 유형 아홉 개에는 그 방어가 충분했다. 문제는 그 뒤에 **통과의 뜻이
반대인 열 번째 판정이 추가된 것**이다.

→ 금지 문구만 창 전체를 감시하고 FAIL 이 나오는 순간 멈춘다. 늦게 나타났다는 사실도
남긴다(`appeared_after_ms`) — 즉시 노출과 구현이 다르다. `slowleak` 변형으로 회귀를
못 박았고, **대기를 끄면 놓치는 것까지** 확인해 무엇 덕분에 잡히는지 테스트가 말하게 했다.

> **한 방향으로만 바꾸는 장치는, 새 판정이 추가될 때마다 방향을 다시 확인해야 한다.**

이 발견과 그때 함께 걷어낸 죽은 코드·거짓 주석·낡은 문서는
[`docs/teaching/17-audit.md`](docs/teaching/17-audit.md) 에 있다.

---

### 14. 자연어 요청을 받으면서, 그 해석이 결함을 덮지 못하게 막았다

계획서 1차 목표 4번은 "사용자가 자연어로 요청하면 실행 가능한 테스트 시나리오로
변환한다" 이다. 이걸 붙이는 순간 이 도구가 지켜 온 성질 하나가 깨진다.

지금까지 케이스 선택에는 LLM 이 전혀 관여하지 않았다. S1 의 해석이 틀리면 케이스가
이상해져서 눈에 띄고, S2 의 제목이 틀려도 판정은 그대로다. 그래서 **"리포트에 없는
결함은 없는 결함"** 이 성립했다.

요청 해석이 케이스를 고르기 시작하면 그게 무너진다.

    모델이 케이스를 잘못 골라서 결함을 못 본 것
    결함이 정말 없는 것
    ── 리포트에서 똑같이 초록불이다

대기(설계 판단 12)와 같은 모양의 문제다. 대기는 FAIL 을 PASS 로만 바꿀 수 있고
그 반대는 없었다. 여기서는 **선택이 FAIL 을 사라지게만 할 수 있고 없던 FAIL 을
만들지는 못한다.** 방향이 한쪽뿐이므로 안전한 쪽이 정해져 있다.

#### 네 가지로 막는다

**1. 모델에게 고르는 일만 시킨다.** 모델이 돌려주는 것은 `case_ids` 목록뿐이다
(`s2_case_generator/selector.py`). 입력값·스텝·기대값은 손대지 못한다. 그래서 해석이
틀렸을 때 나올 수 있는 최악은 "엉뚱한 케이스를 돌렸다" 이고, "엉뚱한 판정을 내렸다"
는 될 수 없다.

**2. 결과는 항상 부분집합이다.** 모델이 목록에 없는 `case_id` 를 돌려주면 무시하고
경고를 남긴다. 실행되지 않은 케이스가 리포트에 나타나면 판정 전체가 거짓이 된다.

**3. 해석 실패는 더 많이 검사하는 쪽으로 넘어진다.** LLM 이 죽거나 없으면 일부만
돌리거나 멈추지 않고 **전체를 실행한다.** 해석에 실패했다는 것은 무엇을 빼도 되는지
모른다는 뜻이고, 그 상태에서 빼는 것은 결함을 덮는 일이다. 적게 고르면 결함이 숨지만
많이 고르면 숨지 않는다.

**4. 0건은 거부한다.** `--only` 필터와 같은 계약이다. 아무것도 못 고르면 예외를
던진다 — 빈 목록으로 진행하면 "전체 0건, 통과율 100%" 리포트가 나오는데 그건
아무것도 검증하지 않은 상태다.

#### 그리고 UI 를 붙인 진짜 이유

CLI 에는 해석 결과를 보여줄 자리가 없다. `--request` 를 주면 케이스가 조용히 좁혀지고,
사용자는 무엇이 빠졌는지 실행이 끝난 뒤에야 리포트에서 본다.

그래서 웹 UI(`prova serve`)는 실행을 **두 단계로 나눈다.**

    POST /api/plan   브라우저를 열지 않고 '무엇을 테스트할지' 만 정한다
    POST /api/run    사람이 승인한 목록으로 실제 실행한다

계획 화면은 고른 케이스와 함께 **제외한 케이스를 전부 보여주고, 도로 켤 수 있게 한다.**
브라우저가 아직 열리지 않았으므로 승인 전에는 아무것도 실행되지 않는다 — 대상 URL 이
없는 곳을 가리켜도 계획은 나온다(`tests/test_server_e2e.py`).

승인 시점에는 모델을 다시 부르지 않는다(`select_by_ids`). 승인과 실행 사이에 모델의
답이 달라지면 사람이 승인한 것과 다른 것이 돌게 된다.

**UI 는 자연어 층의 편의가 아니라 안전장치다.** 그래서 둘을 따로 만들지 않고 한
덩어리로 만들었다.

#### 리포트에도 남는다

2차 경로 보정(설계 판단 10)과 대기 시간(12)을 남긴 것과 같은 이유다 — 도구가 판단을
했으면 그 사실이 리포트에 있어야 한다. 요청 원문, 모델이 밝힌 선택 근거, **제외한
케이스 목록**이 통과율 바로 아래에 들어간다. 제외 목록에는 "통과한 것이 아니라
실행하지 않은 것" 이라고 못 박는다.

---

## 구현 중 드러난 것들

문서로 예측하지 못했고 코드를 돌려서 알게 된 문제들. 각각 대응이 코드에 남아 있다.

### 손으로 조립한 픽스처는 조용히 낡는다

`multi_spec.md`(통합 기획서)를 화면별 기획서에서 손으로 이어 붙이고 있었다. 로그인
기획서에 예시 동작 절을 추가하고 **PDF만 다시 만들자** 통합 문서에는 그 절이 없어서,
로그인 화면 케이스가 9건이 아니라 7건으로 나왔다. 관통 테스트가 숫자로 잡아 줬지만
그건 운이었다 — 케이스 수를 못 박아 두지 않았다면 통합 문서 쪽 검증이 두 건 줄어든 채로
지나갔을 것이다.

→ 조립을 `scripts/make_multi_spec.py`로 옮기고, **낡았는지를 테스트가 확인한다**
(`tests/test_fixture_consistency.py`). 스크립트만 두면 실행을 잊는 것을 막지 못한다.
md뿐 아니라 **PDF에서 뽑은 화면·요소·시나리오가 화면별 PDF와 같은지**도 대조한다 —
파이프라인이 실제로 읽는 것은 PDF다.

### 테스트가 추적 중인 픽스처를 고쳤다

위 테스트를 만들면서 바로 겪었다. `__main__` 경로가 살아 있는지 보려고 조립 스크립트를
subprocess 로 실행했는데, 그 실행이 **통합 문서를 다시 써 버렸다.** 낡음 검출을 확인하려고
화면 기획서에 임시 절을 넣고 테스트를 돌리자, 스크립트가 그 임시 절을 통합 문서에 박아
넣어서 임시 절을 지운 뒤에도 어긋난 상태가 남았다.

→ 스크립트에 `--check`(쓰지 않고 낡음만 알림, 종료 코드 1)를 두고 테스트는 그것만 쓴다.
**테스트는 부작용이 없어야 한다** — 생성 스크립트를 시험할 때 특히 그렇다.

### PDF는 원문을 그대로 복원하지 못한다

```
원문   비밀번호는 8자 이상이며 대문자·특수문자를 각 1자 이상 포함해야 합니다.
추출   '비밀번호는 8자\n이상이며 대문자·\n특수문자를 각\n1자 이상\n포함해야 합니다.'
```

`8자\n이상이며`의 개행은 공백으로 바꿔야 맞고, `대문자·\n특수문자`의 개행은 지워야 맞다.
한글은 문자 단위로도 줄바꿈되므로 텍스트만 보고는 판별할 수 없다.

→ 복원을 포기하고 **비교를 정규형에서** 한다 (`text_utils.loosen`). 공백만 무시하고,
문구 자체가 다르면 반드시 FAIL이 된다.

### 필수 입력 문구는 요소별이 아니라 화면 공통이다

기획서 §2 표의 `에러 메시지`는 형식 검증용 문구이고, "필수 입력 항목입니다."는
§4 실패 조건 표에 화면 단위로 따로 적혀 있다.

→ `ScreenSpec.required_message` 필드를 추가했다. 이 값이 없으면 문구를 억측하지 않고
`error_shown`으로 격하해 '에러가 떴는지'만 확인한다. **억측한 문구는 오탐을 만들고,
오탐이 나오는 QA 도구는 개발자가 신뢰하지 않게 된다.**

### 규칙을 만족하는 값 ≠ 등록된 계정의 값

`rule_expander`가 만든 `Aa1!aaaa`는 비밀번호 규칙을 다 지키지만 등록된 계정의
비밀번호가 아니다. 그래서 정상 케이스가 **구현 결함 없이** 실패했다.

→ `UIElement.sample_value` 필드를 추가하고, 기획서 §5 테스트 계정을 정상 케이스에 쓴다.

### `guided_json`은 오류 없이 조용히 무시된다

vLLM 0.24에서 `guided_json`이 제거됐는데, **에러가 나지 않는다.** 요청한 필드명
(`screen_name`)이 아니라 모델이 지어낸 이름(`screenName`)이 돌아오고 응답이 마크다운
코드펜스로 감싸진다. 스키마가 강제되지 않으면 S1이 `constraints` 키를 흔들고, 그러면
`rule_expander`가 규칙을 인식하지 못해 **검증이 조용히 무력해진다.**

→ OpenAI 표준 `response_format={"type": "json_schema", ...}`로 교체했다. 표준이라
버전 업그레이드에 강건하고, `prova check`는 연결뿐 아니라 **필드명을 대조해 스키마가
실제로 걸렸는지**까지 확인한다.

### mock 백엔드는 조용히 쓰이면 안 된다

vLLM 연결 실패 시 mock으로 자동 폴백하면, 아무 추론도 하지 않은 리포트가 정상
결과처럼 보인다. QA 도구에서 가장 위험한 실패다.

→ 연결 실패는 명확한 오류로 알리고, mock은 `--backend mock`으로 직접 고를 때만 쓴다.
리포트 HTML 상단에도 경고를 띄운다.

### 7B는 표의 행을 조용히 빠뜨린다 — 그리고 그게 전 케이스 오탐이 된다

회원가입 화면(요소 7개)에서 로컬 7B가 **버튼 행을 빠뜨렸다.** 검증 규칙과 에러
메시지가 모두 `-`인 행이라 중요하지 않다고 판단한 것으로 보인다. 로그인 화면
(요소 3개)에서는 일어나지 않았다.

제출 버튼이 없으면 폼이 제출되지 않는다. 그러면 에러가 뜰 일도 없어 **모든 위반
케이스가 FAIL**이 되고, 정상 케이스도 이동하지 않아 FAIL이 된다. 원인은 추출인데
리포트는 "구현이 전부 틀렸다"고 보고한다. 출력 길이 제한 때문이 아니었다 — 응답은
1,635자로 상한에 한참 못 미쳤다.

→ 두 겹으로 막는다.
1. **예방**: `pdf_parser`가 표에서 읽은 요소 ID 목록을 프롬프트에 직접 넣는다
   (`declared_element_ids`). 표의 몇 번째 열이 ID인지는 괘선으로 정해지므로 추론할
   여지가 없다. **코드가 확실히 아는 사실을 추론에 맡기지 않는다.**
2. **검출**: 추출 결과를 그 목록과 대조해 빠진 요소를 경고한다
   (`structural_warnings`). 버튼이 하나도 없으면 따로 경고한다.

예방만 두면 모델·버전이 바뀔 때 조용히 다시 깨진다. 검출만 두면 매번 깨진 리포트를
받는다. 둘 다 필요하다.

### few-shot 예시를 바꾸면 다른 필드의 정확도가 함께 움직인다

회원가입 화면을 추가하며 프롬프트의 few-shot 예시를 교체했다. 원래 예시가 회원가입
화면이었는데, 그대로 두면 회원가입 정확도 측정이 **예시를 베낀 결과**를 재는 셈이라
'비밀번호 변경'이라는 다른 화면으로 바꿨다.

그 교체가 **로그인의 `sample_value` 추출을 깨뜨렸다.** 새 예시에서 `sample_value`가
대부분 `null`이었던 것이 원인으로 보인다. 정상 케이스가 등록되지 않은 계정 값을 쓰게
되어 구현 결함 없이 실패하는, 이미 한 번 겪은 오탐이 되돌아왔다.

→ 예시의 데이터 표를 2열로 만들어 '열 제목 = 라벨' 대응을 보이게 하고, 두 요소에
실제 값을 채웠다. 프롬프트 설명도 표 제목 예시를 열거해 강화했다.

**교훈은 프롬프트가 전역 상태라는 것이다.** 한 화면을 위해 고친 프롬프트가 다른
화면의 다른 필드를 조용히 망가뜨린다. 화면별 골든 테스트를 모두 돌려야 확인된다.

### element_id에 공백이 섞여 들어온다

7B가 `password_confirm`을 `password_confir m`으로 냈다. 그러면 `same_as` 참조가
어긋나 교차 필드 위반 케이스가 아예 생성되지 않고, `case_id`에 공백이 들어가
스크린샷 경로도 깨진다.

→ `element_id`의 공백을 지우고 `same_as` 참조도 함께 고친다. 판정 강도를 낮추는
종류의 보정이 아니라 표기 복원이므로(슬러그에 공백이 들어갈 여지가 없고 고칠 방법도
하나뿐이다) 경고 없이 처리한다. 매 실행마다 뜨는 경고는 정작 중요한 경고를 묻는다.

### 표에 그대로 적힌 사실을 LLM에 맡기면 안 된다 ← 가장 값진 교훈

화면을 늘리는 동안 **같은 종류의 실패를 네 번** 겪었다.

| 무엇을 | 7B가 한 일 | 결과 |
|---|---|---|
| 요소 목록 | 7행 표에서 버튼 행을 생략 | 폼이 제출되지 않아 전 케이스 FAIL |
| `screen_id` | `화면 ID \| search`를 두고 `product_search`를 지어냄 | `case_id`·스크린샷 경로가 어긋남 |
| `sample_value` | few-shot 표 제목을 바꾸자 두 번 놓침 | 정상 케이스가 오탐 FAIL |
| `scenarios` | 없는 시나리오를 창작, 실패 조건 표의 행을 가져옴 | 없는 기대를 요구해 오탐 |

넷 다 **표에 그대로 적혀 있는 값**이다. 추론할 것이 없다. `pdfplumber`가 이미 괘선으로
열을 갈라 읽고 있는데, 그걸 다시 LLM에게 "읽어 줘"라고 부탁하고 있었던 것이다.

→ 프롬프트를 고치는 대신 `pdf_parser`가 결정적으로 읽어 쓴다.

```python
doc.declared_element_ids()    # 요소 ID 목록      -> 프롬프트에 주입 + 누락 경고
doc.declared_screen_meta()    # 화면 ID / 경로    -> 프롬프트에 주입 + 불일치 경고
doc.declared_sample_values()  # 라벨 -> 예시값    -> 프롬프트에 주입 + 누락 경고
doc.declared_scenarios()      # 입력-결과 짝      -> 이 값을 그대로 쓴다
```

마지막 것은 프롬프트 주입에서 멈추지 않고 **결과를 대체한다.** 보정이 아니라 애초에
LLM이 판단할 일이 아니었기 때문이다 — 열 제목에서 요소 ID를 찾는 것은 조회이고, 셀 값은
그대로 옮기면 된다. LLM이 다른 답을 냈다는 사실은 경고로 남겨, 프롬프트가 나빠지고 있는지
알 수 있게 한다.

**표를 고르는 기준이 캡션이 아니라 열 제목이라는 점**이 이 방식을 견고하게 만든다.

```
열 제목이 전부 요소 라벨    -> 요소별 예시값 표 (sample_value)
열 제목이 일부만 라벨       -> 입력-결과 짝 표 (scenarios)
라벨이 하나도 없음          -> 개요·실패 조건 등 다른 표
```

기획서마다 표 제목이 '테스트 계정'/'입력 예시 데이터'/'예시 검색'으로 달라도 이 기준은
흔들리지 않는다. 실제로 이 기준 하나가 검색 기획서의 두 표를 정확히 갈라냈다.

**LLM에 남는 일은 자연어 판단뿐이다** — "8자 이상"을 `min_length: 8`로 옮기는 것,
"비밀번호와 동일"을 `same_as`로 옮기는 것. 그건 표 안에 구조로 적혀 있지 않다.

---

## 아직 안 한 것

명세서 §0-2 의 **1차 범위는 닫혔다** — 6단계 파이프라인과 네 화면(로그인·회원가입·검색·
비밀번호 찾기), 그리고 입력 검증 규칙 확인. 2차 항목 중 **Self-Healing 재탐색**(VLM)과
**실패 원인 자동 분류**(규칙 기반)도 앞당겨 끝냈다.

계획서 1차 목표 네 개도 모두 닫혔다. 마지막이던 **자연어 → 테스트 시나리오 변환**은
`--request` 와 `prova serve` 로 붙였고, 해석이 결함을 덮지 못하게 하는 네 가지 장치를
함께 넣었다(설계 판단 14).

남은 것을 값 순서로 적는다.

| 남은 것 | 왜 아직인가 |
|---|---|
| **실물 기획서로 S1 확인** | **다음 최우선.** 외부 대기 중. 훼손 6종으로 어떻게 깨지는지는 측정했지만(설계 판단 11) 그건 우리가 상상한 여섯 가지다 |
| **요청 해석 재현율 끌어올리기** | 실측: 튜닝 세트 9/10, 홀드아웃 6/9(2026-08-20, orders 항목 추가). 흐름 지목·건수 검증·정렬 지목("최신순으로 나오는지" 가 `orders-sorted` 를 빠뜨림, 설계 §5-5 미달)에서 빠뜨린다 — 프롬프트로는 한계이고, 화면별 확인 범위 표시가 방어선. 실물 기획서가 오면 재측정 (`scripts/probe_request_selection.py`) |
| 로그인된 세션 재사용 | 사내 화면을 테스트하려면 `storage_state` 저장·재사용 경로가 필요하다. 확장프로그램 없이 풀 수 있다(설계 판단 14) |
| 확장프로그램 트리거 | 현재 탭 URL 을 `localhost:7007` 로 넘기는 얇은 것. 확장은 Playwright 도 LLM 도 못 돌리므로 웹 UI 의 대안이 아니라 트리거다 |
| VLM 2차 경로 오탐 막기 | 실측 오탐 7/10(2026-08-22). 신뢰도 관문은 아무것도 못 걸렀다. (1) 시험지에 DOM 스냅샷을 굳혀 `_require_actionable` 통과율을 잰다 (2) 좌표 아래 요소의 접근성 이름이 기획서의 **다른** 요소 라벨이면 탐지 실패로 돌린다. §9 IoU 52.5% 는 상자 크기 문제라 모델 교체 없이는 안 오른다 — 적중 90% 를 병기해 보고한다 |
| 표 헤더 후보 넓히기 | `항목`·`필드`·`구분` 등. 실물 문서를 본 뒤 결정한다 — 미리 넓히면 다른 표를 요소 표로 잘못 읽는다 |
| 두 실물 모델(LLM+VLM)의 한 실행 관통 | 각각은 측정했다(설계 판단 10). MIG 조각이 9.50 GiB 라 동시 적재가 불가능하고, 서버를 바꾸는 사이에 추출 결과를 보관하는 경로가 없다 |
| `screen_name` 과 `<title>` 대조 | 일부러 미룬다 — 오탐 위험이 높다 |
| 화면 3개 이상을 지나는 흐름 | 미지원. 지금 흐름은 두 화면을 잇는다 |
| 시맨틱하지 않은 표 마크업 | aria-label 없는 `<th>`/`<td>` 로만 짠 표는 지금의 반복 요소 탐지(라벨 -> role=list/row/listitem)가 닿지 않는다. 주문조회 SUT 는 `<ul aria-label>` + `<li>` 로 만들어 이 한계를 피해 갔다 — 실물 화면이 순수 `<table>` 로 오면 탐지 경로를 넓혀야 한다 |
| Figma 연동 · locator 캐시 · 병렬 실행 · CI 연동 | 없어도 명제를 증명하는 데 지장이 없다 |

### GPU 제약 — 실측으로 확정됐다

착수 전에는 추정이었고 이제 실측값이 있다.

```
MIG 1g.10gb 파티션    9.50 GiB
7B (UTIL=0.90)        8.30 GiB 사용
남는 것               1.20 GiB
Qwen2.5-VL-3B-AWQ     3.17 GiB (가중치만)
```

**동시 적재는 불가능하다.** 7B 의 `UTIL` 을 최소선까지 낮춰도 가중치를 겨우 덮는 정도다.

그런데 파이프라인을 보면 **LLM 과 VLM 이 동시에 필요한 시점이 없다** — LLM 은 S1,
VLM 은 S3 다. 그래서 시간을 나눠 쓰고, 그 방식으로 S3 정확도를 측정했다(50개 채점, 적중 90%).
한 실행에서 둘을 잇는 것만 남았다.
