# 정체 대조 관문 재생 — 77항목 시험지 (2026-08-31)

`scripts/eval_identity_guard.py` 의 출력을 그대로 옮긴 것이다. 모델을 다시 부르지
않는다 — 08-31 채점(`vlm-iou-qwen-vl-2026-08-31.json`)의 좌표·신뢰도를 같은 화면에
재생해 관문만 시험한다.

## 결과

    오탐 9건 중 차단: 9        -> 남은 오탐 0
    정상 보정(적중) 43건 중 오차단: 0   (0 이어야 한다)
    빗나간 좌표 2건 중 차단: 2  (기존 빈곳 관문의 몫)
    재생 제외: 19건 (select·list — 2차 경로가 좌표로 조작하지 않는다)

| 화면 | 없는 요소 | 신뢰도 | 막은 이유 |
|---|---|---|---|
| login-empty | 검색어 | 0.90 | 빈 곳 |
| login-empty | 약관 동의 | 0.80 | 빈 곳 |
| signup-welcome | 가입하기 | 0.80 | **정체 대조** — 아래 요소는 '로그인하러 가기' |
| signup-welcome | 비밀번호 | 0.90 | 빈 곳 |
| search-empty | 비밀번호 | 0.90 | **정체 대조** — 아래 요소는 '검색어' |
| find-empty | 비밀번호 | 0.80 | **정체 대조** — 아래 요소는 '이메일' |
| product-empty | 비밀번호 | 0.90 | **정체 대조** — 아래 요소는 '가격' |
| product-empty | 검색어 | 0.90 | **정체 대조** — 아래 요소는 '상품명' |
| orders-list | 비밀번호 | 0.90 | **정체 대조** — 아래 요소는 '시작일' |

아홉 중 여섯을 **정체 대조**가 막았다. 그 여섯은 좌표 아래에 진짜 요소가 있던
자리라 빈곳 관문만으로는 통과했을 것들이다 — 08-25 에 이 관문을 넣은 이유가
정확히 그 모양이었고, 화면이 늘어난 뒤에도 같은 비율로 성립한다.

## 08-28 의 오차단 3건은 관문 탓이 아니었다

08-28 재생에서 `orders-list`·`orders-empty`·`table-orders` 의 `조회` 가 오차단으로
나왔다. 원인은 시험지가 앱보다 낡은 것이었다 — 상태 select 가 붙어 버튼이 밀렸는데
저장된 좌표는 그 전 것이었다. **시험지를 다시 굳히니 3건이 그대로 0 이 됐다.**

진단이 맞았다는 확인이자, 관문을 고치지 않길 잘했다는 확인이다. 그때 숫자만 보고
`_require_actionable` 을 느슨하게 만들었으면 오탐 9건 중 일부가 새어 나왔을 것이다.

## 이번에도 계측기가 먼저 틀렸다

첫 재실행은 **옛 답안(08-28)을 새 화면에 재생했다.** 시험지-앱 대조
(`require_matching_sut`)는 통과했다 — 시험지와 앱은 맞았으니까. 어긋난 것은
답안이었고, 그건 아무도 안 보고 있었다.

`eval_selector_speed` 는 같은 상황을 `check_same_dataset` 으로 이미 막고 있었다.
관문 재생에만 없었다. 그래서 그 함수를 `build_iou_dataset` 로 옮겨 둘 다 쓰게 했고,
**답안 검사를 앱 대조보다 먼저** 부른다 — 앱이 안 떠 있을 때 '앱에 못 닿는다' 로
먼저 멈추면 답안이 어긋났다는 사실이 가려진다.

세 번째로 같은 모양이었다: 재는 도구가 아니라 재는 대상(또는 답안)이 어긋났고,
결과 표에서는 그 사실이 보이지 않았다.
