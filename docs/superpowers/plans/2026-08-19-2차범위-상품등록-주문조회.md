# 2차 범위(상품등록·주문조회) Implementation Plan

> **✅ 완료 (2026-08-20).** Task 1~13 전부 구현·커밋됐다 — 커밋 범위 `eee7d6d..7547205`
> (13과제 SDD 실행 + 전 브랜치 리뷰 + 수정 웨이브), 실모델 관통은 `8854a24`·`a96a50a`.
> 결과: 상품등록 good 20/20 · bad 12P/8F, 주문조회 good 15/15 · bad 7P/8F, 오탐 0.
> 아래 체크박스는 완료 표시로 바꿨다. 이 문서는 **당시의 계획**이고, 지금 상태는
> `docs/specs/2차범위_설계_상품등록_주문조회.md` 의 v1.2 갱신 내역과 노트 19 를 본다.

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [x]`) syntax for tracking.

**Goal:** 전제 상태(로그인)·데이터 전제(시드 주문)를 가진 두 화면을 파이프라인이 검증하게 한다 — good 전부 PASS, bad 의 심은 결함 4종(P1·P2·O1·O2) 정확 지목, 오탐 0건.

**Architecture:** 기획서 "전제" 절 → `Precondition` → S2 순수 함수가 로그인 setup 스텝 생성 → S4 가 test 스텝보다 먼저 실행 → 실패 시 FAIL+`precondition_failed`. 정렬·합계는 반복 요소를 라벨로 찾아 값을 파싱하는 자기일관 판정(`sorted_desc`·`sum_matches`) — 건수 세기(count_items) 경로의 확장이다.

**Tech Stack:** Python 3.11 · pydantic v2 · FastAPI(SUT) · Playwright · pytest. LLM 은 S1 추출에만.

**Spec:** `docs/specs/2차범위_설계_상품등록_주문조회.md` — 이 계획의 모든 판단 근거. 실행자는 스펙을 먼저 읽는다.

## Global Constraints

- 케이스 생성·판정에 LLM 을 쓰지 않는다 — S2·S5 는 순수 함수 (스펙 §3).
- 규칙 하나당 케이스 하나, 위반값은 그 규칙만 어긴다 (rule_expander 계약).
- Verdict 는 PASS/FAIL 이진 유지. 전제 실패는 FAIL + 분류 `precondition_failed` (스펙 §3-6).
- 판정 불능(요소 0개, 파싱 실패)은 FAIL — 확인하지 않은 초록불 금지 (스펙 §3-6).
- 커밋 메시지는 저장소의 서술형 스타일("제목 — 부제" + 이유 본문) + `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`.
- 각 Task 끝에 해당 테스트 통과 확인, Phase 끝에 `uv run pytest` 전체 회귀(터널 없으면 실모델 69개 skip 은 정상).
- 모든 새 코드 주석·문구는 한국어, 기존 파일의 서술 밀도를 따른다.

---

## Phase A — 상품등록 (전제 기반)

### Task 1: 데이터 계약 — Precondition·setup_steps·phase

**Files:**
- Modify: `src/prova/models.py` (ScreenSpec 근처, TestCase, StepResult)
- Test: `tests/test_precondition_model.py` (Create)

**Interfaces:**
- Produces: `Precondition(requires_login: bool, account_email: str, account_password: str, login_screen_id: str = "login")`, `ScreenSpec.precondition: Optional[Precondition]`, `TestCase.setup_steps: list[TestStep]`, `StepResult.phase: Literal["setup","test"] = "test"`

- [x] **Step 1: 실패하는 테스트 작성**

```python
# tests/test_precondition_model.py
"""전제 계약이 직렬화 왕복에서 깨지지 않는지 고정한다.

golden(json) <-> 모델 왕복이 S1 정확도 측정의 기반이므로, 필드가 기본값으로
사라지면 골든 대조가 조용히 무력해진다.
"""
from prova.models import Precondition, ScreenSpec, StepResult, TestCase


def test_전제가_골든_왕복에서_보존된다():
    spec = ScreenSpec(
        screen_id="product", screen_name="상품 등록", url_path="/product",
        precondition=Precondition(
            requires_login=True,
            account_email="seller@test.com", account_password="Seller1!"),
    )
    again = ScreenSpec.model_validate(spec.model_dump())
    assert again.precondition is not None
    assert again.precondition.account_email == "seller@test.com"
    assert again.precondition.login_screen_id == "login"


def test_전제가_없는_화면은_None_이다():
    spec = ScreenSpec(screen_id="login", screen_name="로그인", url_path="/login")
    assert spec.precondition is None


def test_스텝결과의_phase_기본은_test_다():
    r = StepResult(seq=1, action="navigate")
    assert r.phase == "test"
```

- [x] **Step 2: 실패 확인** — `uv run pytest tests/test_precondition_model.py -q` → ImportError(Precondition)
- [x] **Step 3: 구현** — models.py 에 스펙 §2 의 코드 그대로 추가. `Precondition` 은 `ScreenSpec` 바로 위에 두고 docstring 에 "지금은 로그인 하나만 표현한다. kind 일반화는 두 번째 전제가 실재할 때(스펙 §7)" 를 남긴다. `StepResult` 에는 `phase: Literal["setup", "test"] = "test"` (주석: 전제를 세우는 스텝인지 — 리포트가 구분해 보여준다).
- [x] **Step 4: 통과 확인** — 같은 명령 → 3 passed. `uv run pytest tests/test_spec_type_conformance.py -q` 도 통과(스키마 회귀).
- [x] **Step 5: Commit** — `git add src/prova/models.py tests/test_precondition_model.py && git commit` (메시지: "전제를 표현할 자리를 만든다 — 로그인 하나만, 일반화는 하지 않는다")

### Task 2: rule_expander — `numeric` 규칙

**Files:**
- Modify: `src/prova/s2_case_generator/rule_expander.py` (RULE_LABELS:69, satisfies:127, _violation_value:269, valid_value_for:186)
- Test: `tests/test_rule_expander.py` (Modify — 기존 스타일에 클래스 추가)

**Interfaces:**
- Produces: constraints 키 `numeric: true` — `satisfies` 가 검사하고 `_violation_value` 가 위반값 `"만원"` 을, `valid_value_for` 가 정상값(숫자 문자열)을 낸다.

- [x] **Step 1: 실패하는 테스트 작성** (tests/test_rule_expander.py 에 추가)

```python
class TestNumeric:
    """가격·재고수량의 '숫자만 입력 가능' 규칙 (스펙 §2-1).

    format 은 이메일에 묶여 있고 pattern 의 일반 정규식 위반값 생성은
    결정적으로 만들 수 없다. numeric 은 위반값이 자명하다.
    """

    def test_숫자만이_만족한다(self):
        assert satisfies("12000", {"numeric": True})
        assert not satisfies("만원", {"numeric": True})
        assert not satisfies("12,000", {"numeric": True})  # 입력값은 순수 숫자만

    def test_위반값은_그_규칙만_어긴다(self):
        el = UIElement(element_id="price", type="input", label="가격",
                       required=True, constraints={"numeric": True})
        vios = violations_for_element(el)
        v = next(x for x in vios if x.rule == "numeric")
        assert not satisfies(v.value, {"numeric": True})
        assert satisfies(v.value, {})  # 다른 규칙이 없으니 나머지는 자동 충족

    def test_정상값은_숫자다(self):
        el = UIElement(element_id="price", type="input", label="가격",
                       constraints={"numeric": True})
        assert valid_value_for(el).isdigit()
```

- [x] **Step 2: 실패 확인** — `uv run pytest tests/test_rule_expander.py -q -k Numeric` → FAIL
- [x] **Step 3: 구현**
  - `RULE_LABELS` 에 `"numeric": "숫자만 입력"` 추가.
  - `satisfies` 에 분기 추가: `elif key == "numeric" and expected: if not value.isdigit(): return False`
  - `_violation_value` 에 분기: `if rule == "numeric": return "만원"` (주석: 숫자가 하나도 없는 짧은 한국어 — 위반이 자명하고 리포트에서 읽힌다).
  - `valid_value_for` 에서 constraints 에 `numeric` 이 있으면 숫자만으로 값 구성 (기존 문자종류 조합 로직에 numeric 우선 분기: `if element.constraints.get("numeric"): return "12000"` — min/max_length 가 함께 있으면 그 길이에 맞춰 `"1" * n` 을 쓴다).
- [x] **Step 4: 통과 확인** — `uv run pytest tests/test_rule_expander.py -q` 전체 통과(기존 회귀 포함).
- [x] **Step 5: Commit** — "숫자 규칙을 전개한다 — 위반값이 자명한 규칙만 코드가 만든다"

### Task 3: S2 — 전제 스텝 생성과 역방향 가드 케이스

**Files:**
- Create: `src/prova/s2_case_generator/precondition.py`
- Modify: `src/prova/s2_case_generator/generator.py` (generate_cases:185 — 케이스 조립 후 setup 부착), `src/prova/s2_case_generator/__init__.py`
- Test: `tests/test_precondition_steps.py` (Create)

**Interfaces:**
- Consumes: Task 1 의 `Precondition`, `TestCase.setup_steps`
- Produces: `expand_precondition(pre: Precondition, doc: SpecDocument) -> tuple[list[TestStep], list[str]]` (스텝, 경고), `guard_case(spec: ScreenSpec, pre: Precondition, doc: SpecDocument, seq: int) -> Optional[TestCase]`, generator 가 전제 화면의 모든 케이스에 setup_steps 를 붙이고 guard 케이스를 더한다.

- [x] **Step 1: 실패하는 테스트 작성**

```python
# tests/test_precondition_steps.py
"""전제 스텝은 코드가 만든다 (스펙 §3-2·§3-3). LLM 은 관여하지 않는다."""
import pytest
from prova.models import Precondition, ScreenSpec, SpecDocument, UIElement
from prova.s2_case_generator.precondition import expand_precondition, guard_case

LOGIN = ScreenSpec(
    screen_id="login", screen_name="로그인", url_path="/login",
    elements=[
        UIElement(element_id="email", type="input", label="이메일"),
        UIElement(element_id="password", type="input", label="비밀번호"),
        UIElement(element_id="submit", type="button", label="로그인"),
    ])
PRODUCT = ScreenSpec(
    screen_id="product", screen_name="상품 등록", url_path="/product",
    precondition=Precondition(requires_login=True,
                              account_email="seller@test.com",
                              account_password="Seller1!"))
DOC = SpecDocument(screens=[LOGIN, PRODUCT])


class TestExpand:
    def test_로그인_스텝_네_개를_라벨로_만든다(self):
        steps, warnings = expand_precondition(PRODUCT.precondition, DOC)
        assert [(s.action, s.target) for s in steps] == [
            ("navigate", "/login"), ("input", "이메일"),
            ("input", "비밀번호"), ("click", "로그인")]
        assert steps[1].value == "seller@test.com"
        assert steps[2].value == "Seller1!"
        assert warnings == []

    def test_로그인_화면이_문서에_없으면_스텝_없이_경고한다(self):
        doc = SpecDocument(screens=[PRODUCT])
        steps, warnings = expand_precondition(PRODUCT.precondition, doc)
        assert steps == [] and any("login" in w for w in warnings)


class TestGuard:
    def test_역방향_케이스는_setup_없이_리다이렉트를_기대한다(self):
        case = guard_case(PRODUCT, PRODUCT.precondition, DOC, seq=16)
        assert case.setup_steps == []
        assert case.case_id == "product-precondition-guard-016"
        assert case.expected.type == "redirect"
        assert case.expected.url_contains == "/login"
        assert [s.action for s in case.steps] == ["navigate"]


class TestGeneratorWiring:
    def test_전제_화면의_모든_케이스에_setup_이_붙는다(self):
        from prova.s2_case_generator.generator import generate_cases
        cases = generate_cases(PRODUCT, doc=DOC)   # 실제 시그니처에 맞춘다
        normal = [c for c in cases if "precondition-guard" not in c.case_id]
        assert normal and all(len(c.setup_steps) == 4 for c in normal)
        guards = [c for c in cases if "precondition-guard" in c.case_id]
        assert len(guards) == 1 and guards[0].setup_steps == []
```

- [x] **Step 2: 실패 확인** — `uv run pytest tests/test_precondition_steps.py -q` → ImportError
- [x] **Step 3: 구현** — `precondition.py`:

```python
"""전제(로그인)를 세우는 스텝을 만든다 — 순수 함수, LLM 없음 (스펙 §3-2).

라벨은 로그인 화면 ScreenSpec 에서 가져온다. 흐름이 값을 라벨로 잇는 것과
같은 원리다 — 탐지는 라벨로 한다는 규칙을 여기서도 지킨다.
"""
from __future__ import annotations
from typing import Optional
from prova.models import (Expectation, Precondition, ScreenSpec,
                          SpecDocument, TestCase, TestStep)


def _find_label(screen: ScreenSpec, element_id: str) -> Optional[str]:
    for el in screen.elements:
        if el.element_id == element_id:
            return el.label
    return None


def expand_precondition(
    pre: Precondition, doc: SpecDocument,
) -> tuple[list[TestStep], list[str]]:
    if not pre or not pre.requires_login:
        return [], []
    login = next((s for s in doc.screens
                  if s.screen_id == pre.login_screen_id), None)
    if login is None:
        return [], [f"전제가 가리키는 로그인 화면({pre.login_screen_id})이 "
                    f"문서에 없어 전제 스텝을 만들지 못했습니다."]
    email = _find_label(login, "email")
    password = _find_label(login, "password")
    submit = next((el.label for el in login.elements if el.type == "button"), None)
    if not (email and password and submit):
        return [], [f"로그인 화면({login.screen_id})에서 이메일/비밀번호/제출 "
                    f"요소를 찾지 못해 전제 스텝을 만들지 못했습니다."]
    return [
        TestStep(seq=1, action="navigate", target=login.url_path),
        TestStep(seq=2, action="input", target=email, value=pre.account_email),
        TestStep(seq=3, action="input", target=password, value=pre.account_password),
        TestStep(seq=4, action="click", target=submit),
    ], []


def guard_case(
    spec: ScreenSpec, pre: Precondition, doc: SpecDocument, seq: int,
) -> Optional[TestCase]:
    """'비로그인이면 로그인으로 이동' — 전제 절 자체의 검증 (스펙 §3-3)."""
    if not pre or not pre.requires_login:
        return None
    login = next((s for s in doc.screens
                  if s.screen_id == pre.login_screen_id), None)
    if login is None:
        return None
    return TestCase(
        case_id=f"{spec.screen_id}-precondition-guard-{seq:03d}",
        screen_id=spec.screen_id,
        title="비로그인 접근 시 로그인 화면으로 이동하는지 확인",
        type="negative",
        steps=[TestStep(seq=1, action="navigate", target=spec.url_path)],
        expected=Expectation(type="redirect", url_contains=login.url_path),
    )
```

  generator.py 배선: `generate_cases` 끝에서 (실제 시그니처를 확인해 doc 인자가 없으면 추가한다 — 흐름 생성이 이미 doc 을 받는 방식을 따른다):

```python
    if doc is not None and spec.precondition and spec.precondition.requires_login:
        setup, warnings = expand_precondition(spec.precondition, doc)
        spec.warnings.extend(warnings)
        if setup:
            for c in cases:
                c.setup_steps = [s.model_copy() for s in setup]
            g = guard_case(spec, spec.precondition, doc, seq=len(cases) + 1)
            if g is not None:
                cases.append(g)
```

- [x] **Step 4: 통과 확인** — `uv run pytest tests/test_precondition_steps.py tests/test_generator.py -q`
- [x] **Step 5: Commit** — "전제 스텝은 코드가 만든다 — 라벨로 잇고, 못 만들면 경고한다"

### Task 4: S1 — 전제 절 추출 + 표 교정

**Files:**
- Modify: `src/prova/s1_spec_extractor/extractor.py` (`_apply_declared_*` 함수들 옆에 `_apply_declared_precondition` 추가, `extract_screen_spec:284` 에서 호출), `build_user_prompt:214` (전제 절 안내 한 줄)
- Test: `tests/test_extractor_guards.py` (Modify — 클래스 추가)

**Interfaces:**
- Consumes: `ParsedDocument` (pdf_parser), Task 1 의 `Precondition`
- Produces: 전제 절이 있는 기획서에서 `spec.precondition` 이 채워진다. 계정 이메일·비밀번호는 **표에서 코드로 교정**한다(모델이 틀려도 표가 이긴다 — `_apply_declared_scenarios` 와 같은 패턴).

- [x] **Step 1: 실패하는 테스트 작성** (test_extractor_guards.py 스타일에 맞춰)

```python
class TestPreconditionCorrection:
    """전제 계정은 표가 이긴다 (스펙 §3-1·§8) — 모델이 옮겨 적다 틀려도
    기획서 표의 값으로 덮어쓴다. 옮겨적기 실패는 실측된 실패 모양이다."""

    def test_전제_표의_계정으로_덮어쓴다(self):
        spec = ScreenSpec(screen_id="product", screen_name="상품 등록",
                          url_path="/product",
                          precondition=Precondition(requires_login=True,
                                                    account_email="wrong@x.com",
                                                    account_password="wrong"))
        # '전제' 절의 계정 표 (§5 테스트 계정과 같은 2열 형식)
        table = [["이메일", "비밀번호"], ["seller@test.com", "Seller1!"]]
        _apply_declared_precondition(spec, table)
        assert spec.precondition.account_email == "seller@test.com"
        assert spec.precondition.account_password == "Seller1!"

    def test_전제_절이_없으면_건드리지_않는다(self):
        spec = ScreenSpec(screen_id="login", screen_name="로그인",
                          url_path="/login")
        _apply_declared_precondition(spec, None)
        assert spec.precondition is None
```

- [x] **Step 2: 실패 확인** → ImportError(_apply_declared_precondition)
- [x] **Step 3: 구현** — extractor.py:
  - S1 스키마(ScreenSpec 기반)에 precondition 이 이미 포함되므로 프롬프트에 한 줄 추가: "'전제' 절이 있으면 precondition.requires_login 을 true 로 하고 계정을 옮겨 적으세요. 없으면 null." (build_user_prompt 의 절 안내 부분)
  - `_apply_declared_precondition(spec, account_table)`: 표의 헤더가 `이메일|비밀번호` 이면 첫 데이터 행으로 `spec.precondition` 의 계정을 덮어쓴다. 전제 절 표는 pdf_parser 의 ParsedTable 중 **"전제" 제목 아래 첫 표** 로 찾는다 — 기존에 §5 테스트 계정 표를 찾는 로직이 있으면 그 탐색 함수를 재사용하고, 없으면 절 제목 텍스트 위치 기반 탐색 함수를 하나 만들어 둘 다 쓴다.
  - `extract_screen_spec` 의 기존 `_apply_declared_*` 호출 대열에 추가.
- [x] **Step 4: 통과 확인** — `uv run pytest tests/test_extractor_guards.py -q`
- [x] **Step 5: Commit** — "전제 계정은 표가 이긴다 — 모델의 옮겨적기를 믿지 않는다"

### Task 5: S4·S5·S6 — setup 실행, precondition_failed 분류, 리포트

**Files:**
- Modify: `src/prova/nodes.py` (run_cases:172 — setup 먼저 실행), `src/prova/s5_verifier/assertion_engine.py` (verify:436 — 분류 추가), `src/prova/s6_report/report_builder.py` (CATEGORY_LABELS:39, 화면별 집계 _screens_html)
- Test: `tests/test_precondition_verdict.py` (Create)

**Interfaces:**
- Consumes: Task 1 `StepResult.phase`, Task 3 `TestCase.setup_steps`
- Produces: setup 실패 → test 스텝 미실행, Verdict FAIL + `category == "precondition_failed"`; 리포트 분류표·집계에 반영.

- [x] **Step 1: 실패하는 테스트 작성**

```python
# tests/test_precondition_verdict.py
"""전제 실패는 '이 화면의 결함' 이 아니다 (스펙 §3-6·§3-7).

로그인이 깨지면 로그인 화면 케이스가 이미 FAIL 을 낸다. 상품등록 케이스들의
전제 실패를 그 화면의 결함으로 합치면 같은 결함이 여러 화면의 결함처럼 읽힌다.
"""
from prova.models import Expectation, StepResult, TestCase, TestStep
from prova.s5_verifier.assertion_engine import PageState, verify


def _case():
    return TestCase(case_id="product-price-numeric-001", screen_id="product",
                    title="가격 숫자 검증", type="negative",
                    setup_steps=[TestStep(seq=1, action="navigate", target="/login")],
                    steps=[TestStep(seq=1, action="navigate", target="/product")],
                    expected=Expectation(type="error_shown"))


def test_setup_실패는_precondition_failed_로_분류된다():
    results = [StepResult(seq=1, action="click", target="로그인",
                          status="error", phase="setup",
                          error_code="element_not_found")]
    verdict = verify(_case(), results, PageState(url="x", text=""))
    assert verdict.verdict == "FAIL"
    assert verdict.category == "precondition_failed"
    assert "전제" in verdict.reason


def test_test_스텝_실패는_기존_분류를_유지한다():
    results = [StepResult(seq=1, action="navigate", target="/product",
                          status="ok", phase="setup"),
               StepResult(seq=2, action="input", target="가격",
                          status="error", phase="test",
                          error_code="element_not_found")]
    verdict = verify(_case(), results, PageState(url="x", text=""))
    assert verdict.category != "precondition_failed"
```

- [x] **Step 2: 실패 확인** → FAIL (category 불일치)
- [x] **Step 3: 구현**
  - `assertion_engine.verify` 맨 앞(기존 스텝 실패 분류보다 먼저)에: setup 단계의 error 스텝이 있으면 `category="precondition_failed"`, reason 은 CATEGORY_LABELS 문구와 정합하게 "전제(로그인)를 세우지 못했습니다 — {실패 스텝의 error_detail}".
  - `nodes.run_cases`: 케이스 루프에서 `case.setup_steps` 가 있으면 먼저 `execute_case_steps(ctx, case.setup_steps)` 를 돌리고 결과마다 `phase="setup"` 을 표시. setup 에 error 가 있으면 test 스텝을 건너뛰고 setup 결과만으로 판정. 성공하면 test 결과를 이어 붙여(각 `phase="test"`) 기존 경로 그대로. **주의**: setup 의 스크린샷 파일명이 test 스텝과 겹치지 않게 `execute_case_steps` 호출 간 ctx 의 스텝 번호가 이어지는지 확인하고, 겹치면 setup 결과의 seq 를 음수 대신 접두("s1..s4")로 남기는 쪽을 택한다 — 실행자가 execute_case_steps 내부를 읽고 결정, 결정을 주석으로 남긴다.
  - `report_builder.CATEGORY_LABELS` 에 스펙 §3-7 의 항목 추가. `_screens_html` 집계에서 `category == "precondition_failed"` 인 FAIL 은 결함 수에 합치지 않고 별도 열("전제 미충족 N")로 표시.
- [x] **Step 4: 통과 확인** — `uv run pytest tests/test_precondition_verdict.py tests/test_assertion_engine.py -q`
- [x] **Step 5: Commit** — "전제 실패를 그 화면의 결함으로 세지 않는다 — 같은 결함이 불어나지 않게"

### Task 6: SUT — 세션과 상품등록 화면 (P1·P2)

**Files:**
- Modify: `sut/app.py` (login_submit 성공 시 쿠키 발급, 새 라우트 4개)
- Test: `tests/test_sut_product.py` (Create — SUT 만 검사, 파이프라인 없이 httpx/TestClient)

**Interfaces:**
- Produces: `GET/POST /{good|bad}/product`. good: 비로그인 GET/POST → 303 `/{v}/login`; 상품명(required·max_length 50)·가격(required·numeric)·재고수량(required·numeric) 검증, 성공 시 "상품이 등록되었습니다." bad: **P1** 가격 numeric 검사 없음, **P2** 로그인 가드 없음 — 그 외 good 과 동일. 판매자 계정 `seller@test.com / Seller1!` 을 REGISTERED 양쪽 변형에 추가. 세션: 로그인 성공 시 `Set-Cookie: session_{variant}=ok`(서명 불요 — 테스트 대상일 뿐 보안 대상이 아님을 주석으로), 가드는 쿠키 유무 확인.

- [x] **Step 1: 실패하는 테스트 작성**

```python
# tests/test_sut_product.py
"""SUT 상품등록 — 심은 결함이 심은 곳에만 있는지 (오탐 0건의 전제).

good 과 bad 는 P1·P2 외에 한 줄도 다르면 안 된다. 다르면 FAIL 이 났을 때
'심은 결함인가, 딴 데가 다른가' 를 구분할 수 없다.
"""
import pytest
from fastapi.testclient import TestClient
from sut.app import app

client = TestClient(app, follow_redirects=False)


def _login(variant: str) -> dict:
    r = client.post(f"/{variant}/login",
                    data={"email": "seller@test.com", "password": "Seller1!"})
    assert r.status_code == 303
    return {c: v for c, v in client.cookies.items()}


class TestGuard:
    def test_good_은_비로그인을_로그인으로_보낸다(self):
        client.cookies.clear()
        r = client.get("/good/product")
        assert r.status_code == 303 and "/good/login" in r.headers["location"]

    def test_bad_는_비로그인도_연다_P2(self):
        client.cookies.clear()
        assert client.get("/bad/product").status_code == 200


class TestPriceNumeric:
    def test_good_은_가격_문자를_거부한다(self):
        _login("good")
        r = client.post("/good/product", data={
            "name": "노트북", "price": "만원", "stock": "3"})
        assert "숫자만 입력" in r.text

    def test_bad_는_가격_문자를_통과시킨다_P1(self):
        _login("bad")
        r = client.post("/bad/product", data={
            "name": "노트북", "price": "만원", "stock": "3"})
        assert "상품이 등록되었습니다" in r.text


class TestHappy:
    @pytest.mark.parametrize("variant", ["good", "bad"])
    def test_정상_등록은_양쪽_모두_성공한다(self, variant):
        _login(variant)
        r = client.post(f"/{variant}/product", data={
            "name": "노트북", "price": "1290000", "stock": "3"})
        assert "상품이 등록되었습니다" in r.text
```

- [x] **Step 2: 실패 확인** → 404
- [x] **Step 3: 구현** — sut/app.py 기존 패턴(render_*/variant 분기, 결함 주석 `# P1:`/`# P2:`)대로. 로그인 성공 라우트(good·bad 양쪽)에 `resp.set_cookie(f"session_{variant}", "ok")` 추가 — 기존 로그인 케이스는 쿠키를 무시하므로 회귀 없음. 폼 HTML 은 기존 render 함수들의 마크업 관례(label for=, role="alert" 에러 영역)를 그대로 따른다.
- [x] **Step 4: 통과 확인** — `uv run pytest tests/test_sut_product.py tests/test_pipeline_e2e.py -q` (로그인 회귀 포함)
- [x] **Step 5: Commit** — "SUT 에 세션이 처음 생긴다 — 상품등록과 심은 결함 P1·P2"

### Task 7: 상품등록 기획서 픽스처 + E2E 수용

**Files:**
- Create: `fixtures/specs/product_spec.md`, `fixtures/specs/product_spec.golden.json`, `fixtures/specs/product_badlogin_spec.md`(+golden — 수용 3용, 전제 비밀번호만 `Wrong1!`)
- Modify: 없음 (`make_spec_pdf.py` 는 *.md 자동 수집)
- Test: `tests/test_product_e2e.py` (Create)

**Interfaces:**
- Consumes: Task 1~6 전부
- Produces: 수용 기준 1(good PASS)·2(P1·P2 지목, 오탐 0)·3(전제 실패 분류)

- [x] **Step 1: 기획서 작성** — `product_spec.md` (login_spec.md 절 구조 그대로):

```markdown
# 상품 등록 화면 기획서

## 1. 화면 개요

| 항목 | 값 |
|---|---|
| 화면 ID | product |
| 화면 이름 | 상품 등록 |
| 경로 | /product |

## 2. UI 요소 정의

| 요소 ID | 유형 | 라벨 | 필수 | 검증 규칙 | 에러 메시지 | 안내 문구 |
|---|---|---|---|---|---|---|
| name | input | 상품명 | Y | 최대 50자 | 상품명은 50자 이내로 입력해주세요. | 상품명을 입력하세요 |
| price | input | 가격 | Y | 숫자만 입력 가능 | 가격은 숫자만 입력할 수 있습니다. | 숫자만 입력 |
| stock | input | 재고수량 | Y | 숫자만 입력 가능 | 재고수량은 숫자만 입력할 수 있습니다. | 숫자만 입력 |
| submit | button | 등록하기 | - | - | - | - |

## 3. 성공 조건

모든 검증을 통과하면 "상품이 등록되었습니다." 문구를 노출한다.

## 4. 실패 조건

필수 입력이 비어 있으면 "필수 입력 항목입니다." 를 노출한다.

## 5. 전제

이 화면은 판매자 계정으로 로그인한 상태에서 동작한다.
비로그인 상태로 접근하면 로그인 화면으로 이동시킨다.

| 이메일 | 비밀번호 |
|---|---|
| seller@test.com | Seller1! |
```

  S1 이 "숫자만 입력 가능" → `numeric: true` 로 옮기도록 extractor 의 규칙 매핑 표(자연어→constraints)에 항목을 추가하고, golden 은 이 문서 그대로의 기대 ScreenSpec(전제 포함)으로 작성한다. `uv run python scripts/make_spec_pdf.py fixtures/specs/product_spec.md fixtures/specs/product_badlogin_spec.md` 로 pdf 생성.
- [x] **Step 2: 실패하는 E2E 작성** — `tests/test_product_e2e.py` (test_pipeline_e2e.py 의 픽스처 관례 — `sut_base`, `MockLLM.for_spec`, module scope run):

```python
EXPECTED_BAD = {"numeric(price)", "guard"}   # P1 · P2

class TestGood:
    def test_전부_통과한다(self, good_run):
        assert good_run[0].summary["fail"] == 0

class TestBad:
    def test_심은_결함만_지목한다(self, bad_run):
        fails = {v.case_id for v in bad_run[0].cases if v.verdict == "FAIL"}
        assert any("price-numeric" in c for c in fails)      # P1
        assert any("precondition-guard" in c for c in fails)  # P2
        # 오탐 0건: 위 둘 외의 FAIL 이 없다
        assert all(("price-numeric" in c) or ("precondition-guard" in c)
                   for c in fails)

class TestBrokenLogin:
    def test_전제_실패는_따로_분류된다(self, badlogin_run):
        cats = {v.case_id: v.category for v in badlogin_run[0].cases
                if v.verdict == "FAIL"}
        normal = {c: k for c, k in cats.items() if "guard" not in c}
        assert normal and all(k == "precondition_failed" for k in normal.values())
```

  (픽스처 3개: good_run=`/good`+product_spec, bad_run=`/bad`+product_spec, badlogin_run=`/good`+product_badlogin_spec. run_pipeline 호출 형식은 test_pipeline_e2e.py 의 `_run` 그대로.)
- [x] **Step 3: 실패 확인 → 구현 조정 → 통과** — 실패 원인을 하나씩 잡는다(추출·라벨·판정 순). 전부 통과할 때까지 상위 Task 코드 수정 허용(단, 스펙 위반 금지).
- [x] **Step 4: Phase A 회귀** — `uv run pytest -q` 전체.
- [x] **Step 5: Commit** — "상품등록을 처음부터 끝까지 — 전제가 있는 화면의 첫 관통"

---

## Phase B — 주문조회 (정렬·합계)

### Task 8: dom_locator — 반복 요소 텍스트 수집

**Files:**
- Modify: `src/prova/s3_grounder/dom_locator.py` (CollectionCount:307·count_items:330 옆)
- Test: `tests/test_collect_texts.py` (Create — count_items 테스트 파일의 픽스처 방식 재사용; 기존 count 테스트가 쓰는 정적 HTML 로딩 방법을 그대로 따른다)

**Interfaces:**
- Produces: `@dataclass CollectionTexts(status: Literal["ok","absent","ambiguous"], texts: list[str], detail: str)`, `collect_item_texts(page, target: str, hint: UIElement | None) -> CollectionTexts` — count_items 와 같은 탐색 로직으로 항목을 찾고 각 항목의 `inner_text` 를 담는다. 예외를 던지지 않는다(부재는 판정 재료).

- [x] **Step 1: 실패하는 테스트** — 기존 count_items 테스트의 페이지 픽스처를 복제해, `<ul aria-label="주문일">` 아래 `<li>2026-08-15</li>...` 3개에서 `texts == ["2026-08-15", "2026-08-12", "2026-08-09"]` 를 단언. absent 케이스(라벨 없음 → status=="absent") 포함.
- [x] **Step 2~4: 실패 확인 → 구현 → 통과** — count_items 의 컨테이너·항목 탐색을 함수로 공유(추출 리팩터)하고 두 소비자(count_items·collect_item_texts)가 쓰게 한다. 기존 count 테스트 전부 회귀 확인.
- [x] **Step 5: Commit** — "세던 것을 읽는다 — 반복 요소 경로에 값 수집을 얹는다"

### Task 9: 판정 — sorted_desc·sum_matches

**Files:**
- Modify: `src/prova/models.py` (ExpectedType:274 에 두 값, Expectation 에 order_target·sum_row_target·sum_total_target), `src/prova/s5_verifier/assertion_engine.py` (PageState 에 `column_texts: dict[str, CollectionTexts] | None = None`, judge 2개, _JUDGES:424 등록), `src/prova/nodes.py` (_count_for:338 옆에 `_texts_for` — 케이스 type 이 두 값일 때만 수집)
- Test: `tests/test_sorted_sum_judges.py` (Create)

**Interfaces:**
- Consumes: Task 8 `CollectionTexts`
- Produces: `_judge_sorted_desc`·`_judge_sum_matches` — 시그니처는 기존 judge 와 동일 `(Expectation, PageState) -> tuple[bool, str]`. 파싱 규칙 고정: 날짜 `^\d{4}-\d{2}-\d{2}$`, 금액은 숫자·쉼표·'원' 외 문자가 있으면 파싱 실패(스펙 §3-4).

- [x] **Step 1: 실패하는 테스트**

```python
# tests/test_sorted_sum_judges.py
"""정렬·합계는 화면 스스로 모순인지를 본다 (스펙 §3-4). 요소 0개는 FAIL —
0건 목록이 '정렬돼 있다' 로 통과하면 아무것도 확인하지 않은 초록불이다."""
from prova.models import Expectation
from prova.s3_grounder.dom_locator import CollectionTexts
from prova.s5_verifier.assertion_engine import PageState, _judge_sorted_desc, _judge_sum_matches


def _state(**cols):
    return PageState(url="x", text="", column_texts={
        k: CollectionTexts(status="ok", texts=v, detail="") for k, v in cols.items()})

EXP_SORT = Expectation(type="sorted_desc", order_target="주문일")
EXP_SUM = Expectation(type="sum_matches", sum_row_target="금액",
                      sum_total_target="합계")


class TestSorted:
    def test_내림차순이면_통과(self):
        ok, _ = _judge_sorted_desc(EXP_SORT, _state(주문일=["2026-08-15", "2026-08-12", "2026-07-28"]))
        assert ok

    def test_역순이_섞이면_실패하고_어디서_깨졌는지_말한다(self):
        ok, why = _judge_sorted_desc(EXP_SORT, _state(주문일=["2026-08-12", "2026-08-15"]))
        assert not ok and "2026-08-15" in why

    def test_같은_날짜는_허용(self):
        ok, _ = _judge_sorted_desc(EXP_SORT, _state(주문일=["2026-08-15", "2026-08-15"]))
        assert ok

    def test_요소_0개는_실패(self):
        ok, why = _judge_sorted_desc(EXP_SORT, _state(주문일=[]))
        assert not ok and "0" in why

    def test_날짜가_아니면_파싱_실패로_실패(self):
        ok, why = _judge_sorted_desc(EXP_SORT, _state(주문일=["8월 15일"]))
        assert not ok and "파싱" in why


class TestSum:
    def test_합이_맞으면_통과(self):
        ok, _ = _judge_sum_matches(EXP_SUM, _state(
            금액=["1,290,000", "39,000"], 합계=["1,329,000원"]))
        assert ok

    def test_합이_다르면_두_값을_모두_말한다(self):
        ok, why = _judge_sum_matches(EXP_SUM, _state(
            금액=["1,290,000", "39,000"], 합계=["1,290,000원"]))
        assert not ok
        digits = why.replace(",", "")
        assert "1329000" in digits and "1290000" in digits

    def test_행_0개는_실패(self):
        ok, _ = _judge_sum_matches(EXP_SUM, _state(금액=[], 합계=["0원"]))
        assert not ok

    def test_금액에_이상한_문자가_있으면_파싱_실패(self):
        ok, why = _judge_sum_matches(EXP_SUM, _state(
            금액=["삼만원"], 합계=["30000"]))
        assert not ok and "파싱" in why
```

- [x] **Step 2~4: 실패 확인 → 구현 → 통과** — 구현 골자:

```python
_DATE_RE = re.compile(r"^\d{4}-\d{2}-\d{2}$")
_AMOUNT_RE = re.compile(r"^[\d,]+원?$")

def _parse_amount(text: str) -> Optional[int]:
    t = text.strip()
    if not _AMOUNT_RE.fullmatch(t):
        return None
    return int(t.rstrip("원").replace(",", ""))
```

  `_judge_sorted_desc`: column_texts 에서 order_target 을 꺼내 status!="ok" 또는 빈 목록 → FAIL(사유에 개수·detail). 날짜 전부 매칭 안 되면 "파싱" 실패. `all(a >= b for a, b in zip(dates, dates[1:]))` — 깨진 첫 쌍을 사유에. `_judge_sum_matches`: 행 파싱 합 vs 합계 첫 항목 파싱값, 불일치 시 두 값 모두 사유에. `nodes._texts_for` 는 `_count_for` 와 같은 조건부 패턴으로 두 라벨(정렬 1개 / 합계 2개)을 수집해 PageState 로 넘긴다.
- [x] **Step 5: Commit** — "순서와 합을 판정한다 — 요소 0개는 통과가 아니라 실패다"

### Task 10: S2 — 주문 표에서 정렬·합계 케이스 생성

**Files:**
- Modify: `src/prova/models.py` (ScreenSpec 에 `seed_rows: list[dict[str, str]] = []` — '테스트 주문 데이터' 표 원본; 주석: 정렬·합계 케이스 생성의 근거이고 S1 교정으로 채운다), `src/prova/s1_spec_extractor/extractor.py` (`_apply_declared_seed_rows` — Task 4 와 같은 표 교정 패턴), `src/prova/s2_case_generator/generator.py` (표가 있고 기획서 요소에 주문일·금액·합계 라벨이 있으면 sorted_desc·sum_matches 케이스 생성)
- Test: `tests/test_orders_cases.py` (Create)

**Interfaces:**
- Produces: seed_rows 가 있는 화면에서 case_id `orders-sorted-NNN`(type sorted_desc, order_target="주문일")·`orders-sum-NNN`(sum_row_target="금액", sum_total_target="합계") + 기존 경로의 expect_count(5건) 케이스.

- [x] **Step 1: 실패하는 테스트** — ScreenSpec(orders, elements: 주문 목록(list)·주문일(text)·금액(text)·합계(text), seed_rows 5행)에서 `generate_cases` 가 두 케이스를 만들고 target 라벨이 위와 같은지 단언. seed_rows 가 비면 두 케이스가 생기지 않고 경고가 남는지 단언(기획서가 데이터를 제시하지 않으면 검증 불가 — 원칙 그대로).
- [x] **Step 2~4: 실패 확인 → 구현 → 통과**
- [x] **Step 5: Commit** — "기획서 표가 검증을 가능하게 한다 — 데이터 없는 정렬·합계 케이스는 만들지 않는다"

### Task 11: SUT — 주문조회 (O1·O2) + 시드

**Files:**
- Modify: `sut/app.py`
- Test: `tests/test_sut_orders.py` (Create)

**Interfaces:**
- Produces: `GET /{good|bad}/orders` — 로그인 가드는 양쪽 동일(good 과 같음 — 여기 결함은 O1·O2 뿐). 시드는 스펙 §1-2 표 5행을 모듈 상수 `SEED_ORDERS` 로. good: 주문일 내림차순 렌더 + 합계 = 전 행 금액 합(1,859,000). **O1**: bad 는 오름차순 정렬. **O2**: bad 는 합계에서 마지막 행 누락(1,847,000). 마크업: `<ul aria-label="주문 목록">`, 각 행에 `<span aria-label="주문일">`·`<span aria-label="금액">`, 합계는 `aria-label="합계"` — Task 8 수집기가 라벨로 찾는다.

- [x] **Step 1: 실패하는 테스트** — good: 응답 HTML 에서 주문일 순서가 내림차순, 합계 문자열 "1,859,000". bad: 순서 오름차순(O1), 합계 "1,847,000"(O2). 양쪽 모두 행 5개.
- [x] **Step 2~4: 실패 확인 → 구현 → 통과**
- [x] **Step 5: Commit** — "주문조회와 심은 결함 O1·O2 — 시드는 기획서 표 그대로"

### Task 12: 주문조회 기획서 픽스처 + E2E 수용

**Files:**
- Create: `fixtures/specs/orders_spec.md`(+pdf·golden)
- Test: `tests/test_orders_e2e.py` (Create)

- [x] **Step 1: 기획서 작성** — product_spec.md 구조 + §5 전제(같은 판매자 계정) + §6 "테스트 주문 데이터" 표(스펙 §1-2 그대로) + §2 요소 표에 주문 목록(list)·주문일(text)·금액(text)·합계(text) 라벨. golden 에 seed_rows·precondition 포함. pdf 생성.
- [x] **Step 2: 실패하는 E2E** — good: fail==0. bad: FAIL 집합이 정확히 {sorted 케이스, sum 케이스} (오탐 0 단언 방식은 Task 7 과 동일). 리포트 HTML 에 두 케이스의 사유(깨진 날짜 쌍 / 두 합계값)가 실리는지.
- [x] **Step 3: 실패 확인 → 조정 → 통과**
- [x] **Step 4: Commit** — "주문조회를 처음부터 끝까지 — 화면 스스로의 모순을 잡는다"

### Task 13: 자연어 요청·문서·전체 회귀

**Files:**
- Modify: `scripts/probe_request_selection.py` (HELDOUT 에 추가: `("orders", "주문 내역이 최신순으로 나오는지 확인해줘", "정렬 지목", [r"orders-sorted"])`), `README.md` (구조·로드맵·화면 표에 두 화면 추가, "대상 확장" 로드맵 행 제거), `docs/specs/2차범위_설계_상품등록_주문조회.md` (구현 중 달라진 세부 갱신 — 문서 버전 v1.1)
- Test: 전체 회귀

- [x] **Step 1: probe 항목 추가** — 코드만 추가(실행은 vLLM 터널이 있을 때. 없으면 "터널 필요" 를 완료 보고에 명시하고 넘어간다 — 수용 5는 터널 있는 세션에서 확인).
- [x] **Step 2: 문서 갱신** — README 화면 표에 상품등록·주문조회 행(실측 수치는 E2E 결과로), 티칭 노트는 이 계획 범위 밖(다음 세션).
- [x] **Step 3: 전체 회귀** — `uv run pytest -q` → 전부 통과(터널 없으면 69 skip 정상). 웹 UI 수용 6: `uv run prova serve` 를 띄워 Playwright 로 orders_spec 계획 화면에 두 화면 그룹이 뜨는 것을 스크린샷 1장으로 확인.
- [x] **Step 4: Commit** — "2차 범위를 닫는다 — 전제와 자기일관 판정이 파이프라인의 일부가 됐다"

---

## Self-Review 결과 (작성 시 수행)

- 스펙 커버리지: §1→T7·T12, §2→T1·T2·T9·T10, §3-1→T4, §3-2·3-3→T3, §3-4→T9·T10, §3-5→T5, §3-6→T5·T9, §3-7→T5, §3-8→T13, §4→T6·T11, §5 수용 1~4→T7·T9·T12, 5→T13, 6→T13. 누락 없음.
- 타입 일관성: `CollectionTexts`(T8) ↔ `column_texts`(T9), `Precondition`(T1) ↔ T3·T4·T7 사용처 일치 확인.
- 미확정 지점 2곳은 실행자 판단으로 명시(Task 3 generate_cases 시그니처, Task 5 setup 스크린샷 번호) — 자리표시자가 아니라 현장 확인 지시다.
