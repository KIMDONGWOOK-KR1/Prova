# 문서 안내

## specs/ — 개발 명세

| 파일 | 내용 |
|---|---|
| `Prova_서비스명세서.md` | **개발 실무 기준 문서.** 6단계 파이프라인(S1~S6)의 입출력 스키마, 데이터 모델(§3), LangGraph AgentState·노드·분기(§4), 하이브리드 탐지 전략(§5), 실패 분류(§6), 성능 지표(§9), 모델 운영 전략(§10) |

코드에서 `명세서 §5-1`처럼 절 번호로 참조하는 대상이 이 파일이다.

**구현이 명세서와 다른 곳이 네 군데 있다.** 각각 이유가 있으며 [../README.md](../README.md)의
"설계 판단" 절에 정리돼 있다. 명세서를 읽고 코드를 볼 때 그 차이를 먼저 확인할 것.

## reference/ — 배경 문서

| 파일 | 언제 보는가 |
|---|---|
| `하반기_계획서_초안.md` | 프로젝트 목적·일정(§5)·팀 역할(§6)·선행연구(§7). 마크다운이라 검색이 쉽다 |
| `2026년 하반기 WE-Meet Project 운영 계획서_팀 소인배.docx` / `.pdf` | 위 초안의 제출용 최종본 |
| `하반기위밋주제개요서.pdf` | WE-Meet 주제 공모 단계의 개요서 |
| `멘토링보고서_0804.pdf` | ㈜WITCHES AI개발팀 멘토와의 1차 멘토링 기록. **"PDF 명세서부터, 로그인 기능만"이라는 1차 범위가 여기서 나왔다** |

## teaching/ — 티칭 노트

팀원에게 전달할 기술 노트. 마일스톤별로 추가한다.

주제 순서: 데이터 계약(pydantic) → vLLM guided_json 구조화 출력 →
LLM에 맡길 일과 코드로 짤 일의 경계 → Playwright locator 전략 → LangGraph 상태 그래프.

## CHEETAH_SETUP.md

GPU 서버에 vLLM을 올리고 로컬에서 SSH 터널로 붙이는 절차. VRAM 10GiB 계산 근거와
OOM 대응 순서 포함.

---

## 원본 위치

이 문서들은 사본이다. 원본은 다음 경로에 있고, **제출·공유용 갱신은 원본에서** 한다.

```
C:\Users\kdw51\OneDrive\바탕 화면\AIM.소인배\하반기WE-Meet\
```

원본 폴더에 있으나 여기 가져오지 않은 것:

- `회의비/` — 회의비 지출 증빙(행정 문서). 개발과 무관
- `*.hwp` — 같은 내용의 PDF가 있고 파일 하나가 28MB에 달해 저장소를 무겁게 한다
- `_build_docx.py` — 계획서 DOCX 생성 스크립트. 계획서 문서 작업용이며 Prova 코드와 무관

명세서나 계획서를 고쳤으면 원본을 고친 뒤 이 폴더로 다시 복사할 것. 반대 방향으로
하면 원본과 갈라진다.
